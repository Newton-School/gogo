# Implementation progress — not completion evidence

Plan: `gogo-framework-end-to-end`  
Approved architecture: `5b3383ff4fa95ff74897e635f1033351ca799444d7147a44e5e91eae0256d30b`

This is an execution ledger, not a requirements document or an AgentFlow completion record. The approved architecture and immutable manifest remain authoritative. Every task below remains open until all its branches and consumers have verified implementation evidence. Catalog entries and a passing package test are not parity proof.

## Verified checkpoints so far

- `64f0997` adds opt-in `dumpdata`/`loaddata` commands over the scoped fixture service. Explicit database alias, JSON/JSONL format and model selections are required before resources open; parsed selections and IO handles are frozen across configuration/resource callbacks. Loads use stdin, exports use stdout, and successful loads emit a bounded receipt; no file discovery, automatic resource selection or retries occur. A private per-call completion holder retains actual commit/dry-run/export receipts across output, cancellation, wrapper and cleanup failures without retaining provider diagnostics. Root/Data source reviews closed across all eight paths. A regression first reproduced cleanup erasing an uncertain commit result (0.611s); safe runner causes now survive cyclic/oversized cleanup error graphs, and nonconforming context errors are normalized without traversal. Final exact-source management and serialization races passed three times (2.376s/4.899s), strict actual PostgreSQL command races three times (1.983s), app/Async-management/codegen races three times (1.671s/1.679s/1.640s), and six corresponding package vets. Coverage includes pre-resource required flags, post-parse mutation, help rendering, real dry-run/scoped import, denial, duplicate writes, lost commit acknowledgement, failed summary output and failing resource closers. Mappings: `fixtures-data`, `tools-commands`, `tools-command-catalog`, `testing-suite`. Core fixture limitations, automatic scaffold wiring, sequence reset and whole-snapshot verification remain open; standard flag diagnostics are not suitable for secret values.

- `02e97c7` adds explicit scoped JSON/JSONL fixtures with complete non-auto-key, insert-only imports. Export uses deterministic model/key pages in one read-only repeatable-read snapshot and private field allowlists. Strict versioned cells preserve exact integers/decimals/JSON numbers, SQL NULL versus JSON null, binary and microsecond temporal values. Import parses before SQL, owns a durable transaction, runs raw saves without model/default hooks, checks actual deferred constraints/triggers before stored-row/grant fences, and distinguishes confirmed rollback, commit and unknown acknowledgement. Root/Admin source reviews closed across all thirteen paths. Review fixes cover blank-line allocation, complete declared identities, trailing provider JSON, diagnostic provenance, detached INSERT witnesses, closed numeric normalization bounds, delimiter-free record limits and lazy local-time snapshots. Final full serialization races passed three times (4.949s); parser fuzz passed 43,668 executions (11.501s), numeric/rich-cell exactness fuzz 411,467 (10.294s), and vet passed. Root independently passed actual PostgreSQL fixture races three times (2.124s), complete DB/models/ORM/serialization races three times (1.551s/1.524s/2.088s/4.596s), and all five corresponding vets. Native proof covers composite/custom-column round trips, scoped pages, same-transaction grants, duplicate/suppressed writes, deferred foreign keys and trigger effects, dry-run rollback, actual committed data with a synthetic lost reply, late cancellation and committed callback failure. Mappings: `fixtures-data`, `orm-transactions`, `testing-suite`. Auto-key import/sequence reset, natural keys, relations, custom formats, automatic initial data, backup/restore and the next whole-snapshot gate remain open; the opt-in CLI is a separate checkpoint.

- `951bc11` adds explicitly registered typed JSON WebSocket messages over pinned `coder/websocket` 1.8.15. Mandatory handshake/current-session/receive/send grants, exact origin/subprotocol rules, detached bounded requests and envelopes, strict typed schemas and preallocation checks precede effects and output. Bounded queues, rate/time/connection limits, explicit streaming selection, per-handler drain and forced transport closure retain connection permits until all work actually exits. Root/Async/Data source reviews closed; an allocation audit repaired unnecessary nested fixed-composite allocation before the final gate. An exact eleven-path snapshot plus fourteen module metadata paths passed the allocation regression three times (1.655s), all HTTP races three times (7.348s), real-server/router/application-drain integration three times (1.774s), typed-input fuzz with 35,213 executions (11.617s), and HTTP/URLs/app/integration vets. Earlier exact-source envelope, syntax and request fuzz gates passed as recorded during implementation. Module manifests/sums were tidied independently with workspace disabled; no local development artifact hashes or replace paths were retained. Mappings: `http-websocket`, `http-catalog`, `core-lifecycle`, `testing-suite`, `release-modules`. This is not automatic app wiring, browser proof, complete WebSocket conformance, external module publication, or the next whole-snapshot/dependency audit. Callback cooperation and explicit application transaction/idempotency boundaries remain required.

- `69ac608` adds the explicitly selected Memory `PeriodicStore`, using a fake clock with revision/fence/lease checks, bounded detached snapshots and atomic occurrence/intent retention. It drives the existing Beat, relay, worker and result protocols without starting a scheduler or changing production Redis behavior. Native cross-adapter tests cover the shared lifecycle, while the guide records simulator differences rather than claiming blanket Redis parity. Root/Data independent source reviews closed. An exact seven-path committed-base snapshot passed focused Memory/Beat races three times (2.153s/1.761s), strict actual Redis conformance three times (8.534s), all Async/management/testing races (31.757s/1.685s/1.470s), existing and new native periodic/Beat regressions (4.714s), and both module subtree vets. Snapshot fuzz passed 16,131 executions (12.563s). Coverage includes misfires, overlap, DST, current grants, lease recovery, terminal failure, pre-write failure and a genuinely retained occurrence followed by a lost reply. Mappings: `async-beat`, `async-testing`, `async-contracts`, `async-canvas-catalog`, `testing-suite`. This is not durable Memory storage, namespace leadership, complete calendar/worker parity or the next whole-snapshot gate.

- `08ba567` adds optional `db.ConstraintCheckTransaction` and its real PostgreSQL transaction implementation. The explicit check forces pending deferrable constraints and constraint triggers on that exact transaction, never the pool, and leaves them immediate without committing or resetting sequences. It enables fixture dry-run validation to observe failures before rollback; it is not a complete fixture loader. Async independently reviewed all four paths and native provider semantics. An isolated committed-base snapshot plus the four exact paths passed complete Core DB and actual PostgreSQL race suites three times (1.662s/27.697s) and both vets. Native checks cover pool and dedicated-connection transactions, deferred FK denial, constraint-trigger writes, immediate enforcement, cancellation, rollback/commit visibility and finished-transaction refusal. Mappings: `fixtures-data`, `orm-transactions`, `testing-suite`. Fixture imports must perform this check before their final stored-row/grant fences because constraint triggers may change rows; automatic identity synchronization and the next whole-snapshot gate remain open.

- The private HTTP download checkpoint adds explicit GET/HEAD routing over the owner-linked File Service. Bounded detached requests, mandatory request/object grants, canonical metadata identities and ready/link checks precede storage access. Responses use private no-store attachment headers, opaque ETags, valid Last-Modified times, conditional preconditions and bounded single-byte-range handling; every conditional request reauthorizes. Exact file size, initial reads and seeks are checked before headers; later read/write/cancellation/close failures abort the stream without appending an error body. The existing explicit Streaming selector is required. Root/Async independent source reviews closed, including RFC range-unit case and bounded empty-list handling. An isolated committed-base snapshot plus the seven exact frozen paths passed full Files and HTTP races three times (2.833s/7.153s), strict actual PostgreSQL download/owner-service integration three times (3.509s), and all three vets. Four mutational fuzz campaigns passed: range 15,110 inputs, ETag lists 254,883, request ownership 250,937 and modification dates 257,851. Native server coverage proves streaming bypass and client-visible unexpected EOF after partial delivery. Mappings: `files-download`, `http-responses`, `http-streaming`, `testing-suite`. Signed/public links, file deletion, raw upload/quarantine, browser proof and the next whole-snapshot gate remain open.

- The immutable `f9084033becd6f756a18796aa67e76cfe759c4ea` snapshot passed all seven workspace race suites on Go 1.26.8, including strict actual PostgreSQL/Redis integration (192.373s; demo 4.837s), six Admin JavaScript contracts (43.393ms), vet and build. Workspace-disabled packaging/tests passed for all six public modules and a fresh generated consumer with checksum verification, fresh module caches and an explicit cached file proxy. The actual `make audit-dependencies` target passed against the live advisory database: 51 root packages, 18 modules, zero reachable vulnerabilities and zero vulnerable imported packages; the unused `x/crypto/openpgp` module advisory remains as previously recorded. This closes the combined gate for private file ownership, Core task dispatch, exact-key cache invalidation, HTML Update and the Group wait repair. It excludes unfinished HTTP downloads, WebSockets, their new dependency and Memory periodic scheduling. Mappings: `testing-suite`, `testing-contract-matrix`, `release-modules`. All 152 approved tasks remain open; these local checks are not package publication, deployment, browser proof or complete Django/Celery parity.

- `f908403` repairs existing Group Join/JoinOutcomes/Iterate context boundaries without adding duplicate wait APIs. Nonterminal waits with closed Done and nil Err no longer report success or synthesize members; context-method panics/private errors fail safely, and terminal completion checks cancellation while retaining only real partial outcomes. Consumer break and panic remain consumer-owned, with no callbacks after break. Root independent six-path source review closed. Permanent regressions first failed (0.737s); focused existing/new group races passed three times (16.715s), final wait races three times (2.316s), actual Redis waits three times (8.829s), full Async/management races passed (31.911s/1.662s), and broader Redis group/canvas/chord races passed twice (8.434s); both vets passed. A test-only signature-call compile error was corrected before the native passes. Mappings: `async-results`, `async-group`, `async-canvas-catalog`, `testing-suite`. No wait cancellation revokes or retries work; whole-snapshot verification remains separate.

- The HTML Update checkpoint adds an explicit scoped, current-row model form with mandatory change grants, built-in CSRF, bounded URL-form input and intrinsic/composite identity handling. GET/HEAD never writes; POST owns a durable root-row lock and force-update transaction, preserves read-only columns, rejects identity changes and checks the actual stored row after application hooks. Invalid forms render only after a confirmed rollback; success redirects require an observed commit, and committed callback failures never invite automatic retry. Explicit JSON null remains distinct from SQL NULL. Root/Data source reviews closed. A cancellation regression first reproduced invalid keys incorrectly returning 404; final HTTP/forms/templates races passed three times (7.136s/1.625s/1.806s), and actual PostgreSQL Create plus Update races passed three times (2.805s), including a directly observed native lock waiter and composite-key isolation. Update fuzz passed 1,990 executions (11.573s), existing Create fuzz passed 2,630 (11.581s); all four vets passed. Mappings: `http-generic`, `forms-model`, `orm-save`, `orm-transactions`, `security-csrf`, `testing-suite`. Multipart upload, relation forms, optimistic stale-browser detection, visual browser proof and whole-snapshot verification remain open.

- `5435254` adds an explicit immutable, bounded exact-key cache invalidation plan. Namespace-qualified keys are validated and deduplicated before callbacks; sequential Delete reports attempts and acknowledgments separately and retains partial progress after cancellation, panic or a lost reply. OnCommit uses the existing observed transaction/savepoint/rollback and robust-callback semantics, with no automatic replay. Root/Async source reviews closed. Cache race tests passed three times (1.575s), namespace fuzz passed 644,521 inputs (10.634s), and actual PostgreSQL plus Redis commit/rollback/savepoint/lost-reply integration passed three times (1.941s); corresponding vets passed. Mappings: `cache-invalidate`, `cache-access`, `orm-transactions`, `testing-suite`. This is the non-durable branch only: process-crash loss and read-through repopulation remain documented; durable outbox/version invalidation and whole-snapshot verification remain open.

- `d11b5dd` adds the independently importable Core task dispatch port and explicit Async bridge with bounded detached JSON requests/results, portable safe errors and no implicit runtime. A private per-call producer observation distinguishes actual publication attempts/acknowledgments from callback-forged or borrowed acceptance errors; original retry behavior remains unchanged. All four false-acceptance regressions failed before the repair. Root/Admin independent source reviews closed. Focused Core/Async races passed three times (1.608s/4.088s), real Redis dispatch/retry races three times (4.415s), and full Core tasks/Async/management races three times (1.282s/89.832s/1.703s). Request fuzz passed 24,387 inputs (11.599s); vets passed. A workspace-disabled source-linked Core-only consumer passed three times (7.116s) with checksums and no Admin/Async/connector dependency. Mappings: `services-catalog`, `async-register`, `async-publish`, `async-results`, `testing-suite`, `release-modules`. Packaged release, automatic configuration/worker wiring and whole-snapshot verification remain open.

- `d72e637` adds explicit existing-owner private file binding: the approved ten-column `gogo_files` model/migration, canonical bounded owner references, mandatory scope/current grants, prevalidated-byte StoreValidated and authorized Open. A published blob is bound to its existing owner field in one locked durable transaction; replacement retires only exact owned metadata and retains the old blob. Open uses a read-only repeatable-read snapshot and never reaches storage before ready/link/authority checks. Exact publication, unknown publication, unknown commit and confirmed-commit callback failures remain distinct; no failed acknowledgment triggers blind retry or orphan deletion. Root/Async source reviews closed, with fixes for callback-shared schema/time metadata and suppressed committed callback errors. Final Files race tests passed three times (2.520s), strict actual PostgreSQL owner/migration/replacement/denial/drift/commit/concurrency tests three times (2.657s), owner-reference fuzz passed 160,144 inputs (10.662s), and Files/integration vets passed. The first native compile caught an unrelated in-progress Update fixture method, and a later setup run lacked the explicit disposable PostgreSQL DSN; both were corrected without relaxing service requirements. Mappings: `files-upload`, `files-download`, `data-builtin-models`, `services-catalog`, `testing-suite`. Raw upload validation, HTTP download/conditional/signed access, quarantine, destructive cleanup and a new whole-snapshot gate remain open.

- The immutable `d6ca67bd85dd44d546ded9869da76745b585604e` snapshot passed all seven workspace race suites on Go 1.26.8, including strict actual PostgreSQL/Redis integration (188.712s; demo 4.744s), six Admin JavaScript contracts (43.218ms), vet and build. Workspace-disabled packaging/tests passed for all six public modules and a fresh generated consumer, using checksum-enabled fresh module caches and an explicit cached file proxy. The actual `make audit-dependencies` target also passed: 50 root packages, 18 modules, zero reachable vulnerabilities and zero vulnerable imported packages; the unused `x/crypto/openpgp` module advisory remains as previously recorded. This supersedes the pending combined verification for Local Storage, terminal Group Forget, Static and Result context repair. It excludes unfinished Core task dispatch, file metadata/service, HTML Update and cache invalidation sources. Mappings: `testing-suite`, `testing-contract-matrix`, `release-modules`; local verification is not publication, deployment or full Django/Celery parity.

- Independent final Result review found a queued task could falsely complete with a zero value when a malformed context exposed a closed Done channel but nil Err. `d6ca67b` returns unavailable instead; permanent Get/Wait regressions and the surrounding result-boundary races passed three times (1.935s), then the immutable full gate above passed. The two-path final source review closed. Mappings: `async-results`, `testing-suite`.

- The result-context boundary repair captures the whole selected result identity/client before calling context methods, detaches provider observations before post-lookup context callbacks, and uses the existing safe context helper for checks and deferred completion. Standalone regressions first failed (0.704s), reproducing all four operations being retargeted, read metadata mutation, escaped typed-nil/panicking contexts and exposed nonconforming context errors. Fixed focused boundary races passed three times (2.003s); a full Async run with concurrent Core-dispatch sources present passed (30.260s; management 1.655s), and Async vets passed. Root independently reviewed the two-path repair and standalone tests. Mappings: `async-results`, `testing-suite`; the next immutable whole-snapshot gate will separately verify this committed repair without unfinished Core-dispatch or file-service sources.

- The Static checkpoint adds explicit project/app source precedence, bounded discovery, CSS dependency-first SHA-256 hashing, strict immutable manifests, Linux/macOS no-overwrite asset publication and atomic manifest replacement. Existing hashes remain available for older clients. Opt-in `collectstatic`/`findstatic`, escaped static template tags and a DEBUG-only GET/HEAD handler require no database/Redis startup or `Ready` hooks. Root/Data source reviews closed; repairs include mixed-directory EOF rejection, all conditional-header lines, exact configured CDN references, final error redaction and entry-time command flag capture. Final full Static/management/templates/HTTP races passed three times (2.715s/1.913s/1.472s/6.395s), real-filesystem central-command integration passed three times (1.971s), and all five vets passed. Independent CSS fuzz exercised 212,149 inputs (11.374s); manifest round-trip/URL ownership fuzz exercised 186,695 (11.308s). Mappings: `static-collect`, `templates-catalog`, `templates-render`, `tools-commands`, `tools-command-catalog`, `core-bootstrap`, `testing-suite`. This is local macOS verification of the stated Linux/macOS publisher, not browser/Windows proof, automatic scaffold wiring, all CSS/Django flags, production CDN deployment or a new whole-snapshot gate.

- `98dca89` makes the dependency reachability audit reproducible through `make audit-dependencies`, using pinned `govulncheck@v1.7.0`, the live advisory database and all seven workspace package patterns. The scanner remains outside public module requirements. Async independently verified command/exit semantics and help/documentation; the dry-run expands to the same verbose command already executed against `748c3de`. Execution of the newly named Make target is still pending the next frozen combined gate. Mappings: `release-modules`, `testing-suite`.

- The terminal flat-group retention checkpoint adds `GroupResult.Forget`, optional `WorkflowPayloadStore` and scope/revision-checked memory/Redis adapters. Exact graph/child identity, current read/forget grants, terminal states and pins are preflighted before deletion and rechecked at mutation boundaries. Child outputs/progress are released before aggregate/member copies; an explicit retained marker prevents output rehydration while preserving terminal metadata, intent records and replay identities. Partial deletion and unknown acknowledgements remain retryable, not transactional or secure-erasure claims. Root/Data independent source reviews closed. Core/public-example races passed three times (4.720s), actual Redis Forget races three times (2.561s), full Async regression (30.786s; management 1.638s), broader Redis graph/canvas/replacement compatibility (11.060s), and both vets passed. Transition fuzz passed 37,023 executions (11.620s). The final encoded-size/revision preflight prevents child deletion when the marker cannot fit in a valid near-limit graph. Mappings: `async-results`, `async-group`, `async-canvas-catalog`, `async-contracts`, `testing-suite`. Nested canvases, automatic retention scheduling, workflow garbage collection, whole-snapshot compatibility and full result-catalog conformance remain open.

- `e1cab33` adds the public privileged `files.Storage` contract and initial Linux/macOS private local provider. Exact random keys, bounded streaming, SHA-256, atomic no-overwrite hard-link publication, safe error formatting, independent descriptor admission, explicit publication outcomes, caller-owned readers and bounded-memory key pagination have real-filesystem regression coverage. Paths, symlinks/nonregular/public entries, collisions, malformed/no-progress readers, cancellation, panics, root renames and concurrent Close cannot silently overwrite a blob or claim unobserved publication. Full Files races passed three times (2.491s); publication fuzz passed 844 executions (11.481s), and vet passed. Root's first fuzz fixture lacked the required directory mode and was corrected to explicit `0700`, without relaxing production validation. Async independently reviewed all seven paths and reran the final platform-tagged example plus Files races (1.940s) and vet. Mappings: `files-upload`, `services-catalog`, `testing-suite`. This is storage-only: upload/model-form integration, metadata/owner binding, authorized downloads, quarantine and orphan cleanup remain open. No directory-fsync crash durability, hostile-filesystem sandbox, new whole-snapshot gate or full file-service conformance is asserted.

- Go vulnerability scanner `v1.7.0` checked the immutable `748c3de` snapshot's 48 packages, 18 modules and Go 1.26.8 standard library. Both ordinary and verbose scans completed successfully with zero reachable vulnerabilities and zero vulnerable imported packages. One required-module advisory concerns unused, unmaintained `golang.org/x/crypto/openpgp` ([GO-2026-5932](https://pkg.go.dev/vuln/GO-2026-5932)); it has no fixed version, and no scanned package imports or calls it. This is a dated dependency reachability check, not a security certification, and excludes subsequent Files/Static/Group Forget work. Mappings: `release-modules`, `testing-suite`, `testing-contract-matrix`.

- The immutable `748c3de08ca59e26eb79c7fe06f9210eb96a4401` snapshot passed all seven workspace module race suites on Go 1.26.8, including strict actual PostgreSQL/Redis integration (186.042s; demo 4.680s), all six Admin JavaScript contracts (41.666ms), vet and build. A separate gate passed workspace-disabled packaging/tests for all six public modules plus a fresh generated app, typed model generation, migrations and compiled management consumer. This used the explicitly configured cached-dependency file proxy with fresh module caches and checksum verification retained. Tests ran from an ordinary immutable source archive, excluding subsequent static/files/group-retention work. This closes the combined Today/Create/producer/Subquery checkpoint; it is local compatibility evidence, not publication, deployment or complete product conformance.

- `748c3de` raises every module/workspace and generated/source-linked client minimum to Go 1.26.8. The existing Go 1.26.1 installation was affected by the documented [rooted filesystem escape](https://pkg.go.dev/vuln/GO-2026-4970); the [Go release history](https://go.dev/doc/devel/release) also records intervening HTTP/template security repairs. The patched toolchain was selected without changing the global installation or disabling checksums. The generated-client regression failed before repair (0.545s), then codegen/module-helper races passed (1.535s/1.479s), focused vets passed, and the external management consumer passed (9.740s). Explicitly disabling automatic selection rejects the older installed compiler. Independent Admin source review closed. Mappings: `release-modules`, `tools-project`, `testing-suite`; this is not a complete dependency vulnerability audit.

- `a26b8b3` and `4fd6ab1` add lazy `Subquery`, `Exists` and immediate `OuterRef` for scoped correlated reads. Private construction and entry-time backend/alias snapshots precede callbacks; each inner query supplies its own scope, and the compiler emits one statement with quoted disjoint aliases, one parameter sequence, nullable exact scalar outputs and shared expression bounds. Unsupported nesting, grouping, locks, mutations and raw ORM descriptors fail explicitly. PostgreSQL tests prove independent tenant filtering, exact decimals, empty scalar NULL, EXISTS/NOT EXISTS, self-correlation and caller-transaction visibility/rollback. Root/Admin/Async independent source reviews closed. Final full ORM/compiler races passed three times (1.873s/1.453s), actual PostgreSQL Subquery races three times (1.845s), and database/ORM/compiler/PostgreSQL vets plus diff checks passed. Mappings: `orm-expressions`, `orm-select`, `orm-operation-catalog`, `postgres-execute`, `connectors-extend`, `testing-suite`.

- Correlated-read review also reproduced and repaired hidden query nodes reaching annotation encoders, ordinary JSON being incorrectly charged as SQL expression work, and three opaque Filter/CASE ownership mutations. The method-free scanner now memoizes data graphs without executing marshalers or inspecting private state; ordinary type/identity, large JSON and encoder-owned cycle handling are preserved. Parameter-only Output metadata and extracted lazy carriers are rejected before codecs using a sealed internal marker, with separate RED regressions (0.573s/0.504s) and positive codec/JSON compatibility coverage. The final gates above include every repair; this is still a bounded correlated-read checkpoint, not complete ORM or Django conformance. The combined whole-framework and standalone-module gates are pending for this snapshot.

- The independent URL-form creation fuzz campaign passed under the race detector: 2,333 executions, seven new interesting inputs, 13.648s, no failure corpus or production repair. Its 13 retained seeds cover valid submission, HTML text, duplicate/unknown/readonly/identity fields, malformed encoding, NUL/invalid UTF-8, forged CSRF and body overflow. Every success requires one scoped force-insert/commit; rejected input must perform no persistence, preserve private headers and emit no raw script markup. The campaign used the current workspace while Data-owned sources were frozen, following the separate byte-aligned Create HTTP/native gate below; it is not a whole-framework or browser pass. Mappings: `http-generic`, `forms-model`, `security-csrf`, `testing-suite`.

- `8fd76dc` and `6d322eb` add explicit local-scalar/JSON `NewCreateView` forms with named templates, GET/HEAD and authorized OPTIONS, required request/model/proposed-record grants, frozen root scope, mandatory builtin CSRF and bounded URL-form binding. Safe methods and invalid local binding issue no framework SQL; an owned durable ForceInsert transaction applies server preparation, hooks/defaults and final model/constraint validation. A private first OnCommit marker distinguishes rollback, confirmed commit and unknown acknowledgement. Captured RETURNING values and two scoped persisted-state fences reject later callback drift before a sealed local 303. Root review found the initial nil-auto-key guard bug; the private pending-value repair preserves nonnullable declarations and strict persisted identities. Root/Async reviews also drove complete callback metadata detachment, method-free bounds before fingerprint/formatting and captured Factory editable initials. Final independent source reviews closed. A byte-aligned ordinary archive of `a678859` plus exactly eight Create paths and the separate URL-form fuzz seeds passed full HTTP races three times (7.139s), actual PostgreSQL Create races three times (2.360s), both vets and diff checks; concurrent ORM work was excluded. Native cases prove parent/audit atomicity, scope and callback rollback, deferred constraint rejection, actual commit followed by a synthetic unknown reply, postcommit callback failure and late cancellation. Public examples compile; current browser proof and the mutational fuzz campaign remain pending. Mappings: `http-generic`, `http-catalog`, `http-responses`, `forms-model`, `security-csrf`, `orm-save`, `orm-transactions`, `templates-render`, `testing-suite`. This is not update/delete, relation/file/custom-widget, idempotent creation, full edit-view parity or a new whole-snapshot/packaging gate.

- `d25dd25` and `a152060` add opt-in bounded producer recovery to `Client.Enqueue`, `Task.Delay` and retained `AcceptanceError.Retry`. Nil policy remains one attempt; bounded attempts, exponential capped jitter and one entry-time elapsed deadline are separate from execution retries. Private operation snapshots preserve ID/envelope/selected ports across retries; confirmed transport acceptance repairs registration only, while lost acknowledgements remain potentially duplicate at-least-once delivery. Current authority/context fence each write, permanent errors cannot be overridden by a classifier, panics retain truthful acceptance state and internal leased relays remain one-shot. Root review reproduced six retained-result client escapes before the narrow `Task.Delay` fix; Snapshot and post-worker Get now use the originally captured client, including argument/option callback replacement. Root/Admin independent seven-path source reviews closed. Final focused Async races passed three times (1.863s), actual Redis producer races three times (3.145s), and both vets/diff checks passed. An initial helper-name collision was corrected, and vet-driven explicit deadline teardown was followed by the final focused rerun. Mappings: `async-publish`, `async-task-contract`, `async-results`, `testing-suite`. These focused checks do not close the entire publish/catalog task or establish exactly-once delivery, network-failover, whole-snapshot or packaging proof.

- `7abb8d1` and `2d69b5e` add `NewTodayArchiveView`, sharing the existing day-archive execution boundary while deriving the local day and optional publication cutoff from exactly one captured clock instant. Scope is frozen before that clock; private locale configuration, authorization, pagination, navigation budgets and final grants are unchanged. Independent Async source review closed with no findings. A disposable ordinary source archive of `6abf4b0` plus exactly the six Today-owned paths passed full HTTP races three times (6.715s), actual PostgreSQL Today races three times (2.037s), HTTP/integration vets and diff checks. This isolated gate deliberately excluded concurrent unfinished ORM/Create/producer changes; it is not a new mixed-workspace or packaging gate. Unit/native cases cover 23/25-hour days, timezone date rollover, exact-now publication, one-clock HEAD behavior, no-work method/denial paths, private snapshots, invalid/local-year clock failures, scoped absence, unusable dates and final grant revocation. The public example compiles but does not start services. Mappings: `http-generic`, `http-catalog`, `http-responses`, `templates-render`, `orm-select`, `testing-suite`. Week/month/year/index archives, editing views and exhaustive Django compatibility remain open.

- The immutable `5aad4f2a8575ea7f40f10a3daaaaca24edc6f8e2` snapshot passes the complete seven-module race suite with required PostgreSQL/Redis services (native integration 191.228s; demo 4.821s), all six Admin JavaScript contracts (49.842ms), vet/build, and all six independently installed modules plus a fresh generated consumer with workspace overrides disabled. All six module archives were frozen before subsequent implementation began; their fresh caches retained third-party checksum verification through the configured cached file proxy, and owned temporary cleanup completed. This is the latest fully closed packaging-inclusive snapshot, now including the central schema command and scoped DayArchive views. Later correlated-query, publication-retry and HTML-create work is excluded. No browser review, network publication, deployment or complete Django/Celery compatibility is asserted.

- `fcbbaf5` and `6c662bd` add `NewDayArchiveView` with explicit intrinsic Date/DateTime fields, strict local calendar intervals, scoped page/offset pagination and default descending date/primary-key ordering. One captured-now publication cutoff excludes later DateTime instants today; `AllowEmpty` can permit an empty future first page while preserving that cutoff. At most four bounded occupied-neighbor reads share the page's materialization budgets and scope. Navigation-only dates require object and field grants without widening policy records; denied or unusable local-date targets are omitted without farther scanning, and retained grants repeat after rendering. Calendar-derived navigation performs no record discovery. Root/Async independent source reviews closed, including skipped/ambiguous adjacent-date omission. Full HTTP races passed three times (6.444s), final actual-PostgreSQL DayArchive races passed three times (2.050s), and HTTP/integration vets and diff checks passed. The native gate covers composite identities, 23/25-hour days, exact-now cutoff, scoped navigation, empty pages, final grant revocation and first/last navigation-provider failure without partial HTML. An initial native assertion incorrectly expected no previous month for a seeded October 31 row; only that expectation was corrected. The public guide/example explain deferred fields, bounded reads and the absence of an implicit multi-query snapshot. Mappings: `http-generic`, `http-catalog`, `http-responses`, `templates-render`, `orm-select`, `testing-suite`. Today/week/month/year/index archives, edit views and exhaustive Django parity remain open; whole-snapshot/packaging verification is pending for this checkpoint.

- `3945410`, `9238596` and the external-consumer checkpoint add opt-in `management.OpenAPICommand` registration through the existing central command lifecycle. A context-aware selected-router factory, explicit optional resources, argument/help checks before resource startup, private writer/settings/returned-router capture and one complete output write preserve schema metadata and failure boundaries. The factory does not discover opaque whole-site handlers or install authentication. Root/Data source reviews closed, including the callback-time router replacement regression. Final full management/API races passed three times (1.915s/2.295s); the expanded cancellation matrix passed three times (1.697s), with corresponding vets. A checksum-retaining source-linked external consumer passed three times (7.884s): actual compilation and `go run manage.go openapi` produce byte-identical direct-generator output; required configuration, selected resource/Ready/shutdown/close order, pre-resource argument refusal and private resolver/panic/metadata failures are covered. This fixture uses a local module replacement and cached dependencies, not published-archive or new PostgreSQL proof. A failed write/cancellation/cleanup can leave partial or complete output; no retry or appended error document occurs. Mappings: `api-schema`, `tools-commands`, `map-client-journey`, `testing-suite`. Input/mutation schemas, clients, automatic scaffold registration and browsable docs remain open; whole-snapshot/packaging verification is pending for this command checkpoint.
- The immutable `d12d73a916374581d43b2b763b63f23628f8b622` snapshot passes the complete seven-module race suite with required actual PostgreSQL/Redis services (native integration 179.817s; demo 4.856s), all six Admin JavaScript contracts (53.892ms), vet/build, and all six independently installed modules plus a fresh generated consumer with workspace overrides disabled. The isolated file proxy reused verified downloaded third-party artifacts with fresh module caches and checksum verification retained; cleanup completed. All six module archives were frozen before subsequent implementation resumed. This is the latest fully closed packaging-inclusive snapshot and includes scope ownership, serializer schemas, DateDetail, bound read OpenAPI and the Async process-cleanup repair. Subsequent schema-command/day-archive work is excluded. No network publication, deployment, browser review or exhaustive Django/Celery parity is asserted.
- `0681eab`, `c6efc34` and `65009ca` bind ordinary API list/detail handlers and output schemas to the same private resource/store/nested-serializer snapshot, then emit deterministic OpenAPI 3.1.1 for a bounded selected API router. Explicit public/bearer/session OR declarations describe outer authentication without installing it. Literal namespace/prefix provenance, unsupported/opaque/overlapping route rejection, normalized pagination/filter parameters, redaction-aware public output, count/navigation, HEAD/OPTIONS/ETag/304 and public error media are covered. Root/Admin/Data/Async source reviews closed. Final complete API races passed three times (2.593s), including the compile-only public example, all three pagination-mode/count metadata cases and late context fencing; vet passed. Path projection fuzz passed 18,622 executions (10.690s). Actual PostgreSQL OpenAPI/DateDetail races passed three times (2.113s), proving scoped count/navigation, nested public names, hidden/write-only/field-policy omission, original handle replacement, current ETag authorization and separate authentication requirements. Inline output inherits the document dialect rather than carrying invalid nested standalone schema declarations. Mappings: `api-schema`, `api-read`, `api-pagination`, `api-serializers`, `http-routes`, `map-client-journey`, `testing-suite`. This is read-route OpenAPI, not input/mutation schemas, arbitrary route coverage, schema CLI, generated clients or browsable documentation. Offset/cursor metadata tests are not new native navigation proof; whole-snapshot/packaging verification remains pending.
- `ac21ff9` adds scoped DateDetail views on intrinsic Date/DateTime fields, explicit calendar and primary-key decoders, project-local today, half-open intervals across separately resolved local midnights, final authorization and the existing safe template/read boundary. Scope and locale configuration are frozen before decoder callbacks; filtering does not implicitly expose the date field. Invalid/future/scoped-out dates and ambiguous/nonexistent local boundaries return concealed absence; operational/context/row-completion failures return no partial HTML. Root/Data source reviews closed. Full HTTP races passed three times (6.788s), calendar fuzz passed 296,162 executions (9.214s), native DateDetail/OpenAPI races passed three times (2.113s), and corresponding vets passed. The first HTTP attempt found a strict-template test fixture referencing intentionally omitted keys; the fixture was corrected without changing production behavior. Public guide/example cover precise limitations. Mappings: `http-generic`, `http-catalog`, `http-responses`, `templates-render`, `orm-select`, `testing-suite`. Archive/date-navigation/edit views and exhaustive Django timezone compatibility remain open.
- `c5b24c8` adds bounded immutable router descriptions with built-in converter provenance and normalized paginator configuration snapshots. No handler, converter or application callback is sampled. Root/Admin source reviews closed; full URL/pagination races passed three times (1.671s/1.570s; final repeat 1.547s/1.547s), with vets. Mappings: `http-routes`, `api-pagination`, `api-schema`, `testing-suite`.
- `f5849c4` retries transient process-group `EPERM` only inside the original 250 ms observation bound. Success still requires confirmed owned process-group exit; persistent denial, lost ownership and unrelated signal errors remain failures. A matching raw immediate-exit fixture reproduced `clean task process group: operation not permitted` before repair; deterministic transition/persistence cases also failed before repair. Root/Admin independent source reviews closed. Cleanup races passed three times (4.805s), 6,000 raw/public immediate subprocesses passed under the race detector (58.147s), complete Async/management races passed three times (91.789s/1.856s), actual Async Redis/Redis races passed three times (55.223s/4.954s), and corresponding vets passed. Mappings: `async-limits`, `async-worker`, `testing-suite`. This is Darwin runtime evidence, not Linux runtime or whole-snapshot proof.
- The `30bf269` whole-snapshot race attempt failed solely in the Async immediate-exit regression subsequently repaired above. Root, Admin, actual PostgreSQL/Redis, Async Redis and native integration/demo passed (integration 189.400s; demo 4.883s), but this attempt is not a complete passing gate. JavaScript, whole-snapshot vet/build and independent packaging were not run for that attempt.
- Host capacity recovered without additional cleanup by this task. The previously compiler-blocked serializer-output mutational fuzz campaign now passes with the race detector: 22,925 executions, 38 new interesting inputs, 10.599s total. The complete API suite's earlier three-run pass still stands; this focused recovery does not substitute for the whole-snapshot/packaging gate.
- `6043018` and `82ad19c` add deterministic serializer-output JSON Schema 2020-12, explicit bounded custom output declarations, private constructor snapshots, output null/blank/required semantics, policy-sensitive root omission, exact integer/decimal wire contracts and executable public examples. Only public serializer names are emitted; no private model discovery, application callback sampling or reference fetching occurs. Independent Admin/Data source reviews closed, including the separate actual-representation conformance checker and positive/negative controls. The isolated complete API race suite passed three times (2.530s), including examples and six fuzz seeds; vet passed on a separate retry. Both requested mutational fuzz campaigns failed during compiler setup with `no space left on device`, before fuzzing began, so no campaign pass is claimed. Mappings: `api-schema`, `api-serializers`, `api-catalog`, `map-client-journey`, `testing-suite`. Input/write schemas, handler-bound route/authentication metadata, OpenAPI, management commands, clients and browsable documentation remain open. Whole-snapshot and fresh-module verification have not yet run for this checkpoint.
- `2b77400` snapshots scope predicates immediately after root, eager-target and aggregate bridge/target callbacks, and before API root/cache retention. Plain predicate trees, slices and maps cannot be retargeted by later callbacks; opaque custom providers retain their documented read-only ownership. Six ORM regressions reproduced before repair, and the actual PostgreSQL API regression returned a hidden root row or related author before repair. Invalid/cyclic predicates remain rejecting rather than becoming an empty scope. Root/Admin/Data source reviews closed. Isolated full ORM/API races passed three times (1.749s/1.614s), actual PostgreSQL scope/resource/cursor/generic-model races passed three times (2.713s), and corresponding vets passed. Mappings: `orm-select`, `orm-eager`, `orm-expressions`, `api-read`, `api-pagination`, `http-generic`, `testing-suite`. This is a callback-data ownership regression, not proof of a client-controlled exploit or complete authorization conformance; whole-snapshot verification remains pending.
- The immutable `e9e1d81543d3b9f62361d3ed36bf6ce1d87e3644` snapshot passes the complete seven-module race suite with required actual PostgreSQL/Redis services (native integration 195.154s; demo 4.879s), all six Admin JavaScript contracts (43.011ms), vet/build, and all six independently installed modules plus a fresh generated consumer with workspace overrides disabled. The isolated dependency file proxy retained fresh module caches and checksum verification; cleanup completed. This is the latest fully closed packaging-inclusive snapshot and includes the generic Template/Redirect/List/Detail views, iterator cleanup and API response completion below. Subsequent scope-snapshot and serializer-schema work is excluded. No network publication, deployment, browser review or complete Django/Celery parity is asserted.
- The model-view developer guide and external-package constructor examples compile against public backend/store, policy, model registry and URL APIs. Independent review closed; their isolated full HTTP race suite passed three times (6.379s) with vet. They intentionally do not open a service or substitute for the actual PostgreSQL request tests. Mappings: `http-generic`, `map-client-journey`, `testing-suite`.
- `3f59720`, `dccfc3d` and the subsequent List/Detail checkpoint add explicit model-read options, intrinsic scalar/key normalization and bounded scoped HTML views. Model and request authorization, token ceilings, pre-query root scope, private policy-field/primary-key loading, page-size-plus-one selection, exact composite lookups, detached per-callback records and final object/visible-field checks compose without exposing ORM objects to templates. Shared selected-data and JSON parsing budgets prevent per-cell size multiplication. SQL completion failures discard the complete page; nullable schema-level identities and unsupported field/ordering metadata fail closed. Independent source reviews closed. Isolated full HTTP races passed three times for the model contract (5.498s), row boundary (6.534s) and final view composition (6.633s), with vets. Actual PostgreSQL generic-model tests passed three times (1.988s): scoped navigation, hidden/missing/malformed details, exact decimal/integer/JSON-null/time projection, grant loss, method/HEAD behavior, no business-row writes and late deferred-constraint failure without partial HTML. The first native gate found two incorrect test expectations (separators and escaped plus); only assertions needed correction. Mappings: `http-generic`, `http-catalog`, `http-responses`, `orm-select`, `templates-render`, `api-read`, `testing-suite`. Local scalar list/detail support is not all generic views: relational/custom/binary/file fields, count/cursor/date/edit views and exhaustive Django parity remain open. No browser or published-module proof is claimed by these focused gates; whole-snapshot verification remains pending.
- `36b407e` preserves iterator errors through complete cleanup, late cancellation, reentrant close, and provider/factory/decode panics while retaining original panic identity and the existing `All` decoded-prefix contract. Constructor partial readers close, terminal cleanup runs once, and `Get` does not turn a failed stream into an object or absence. Independent source reviews closed; isolated ORM races passed three times (1.785s), actual PostgreSQL completion regressions three times (2.125s), and both vets passed. The deliberately volatile disposable fixture verifies late autocommit FK failure/rollback and caller-owned transaction commit timing, not application read-only behavior. Mappings: `orm-select`, `orm-cardinality`, `orm-transactions`, `testing-suite`. Other query terminal/aggregate catalogs remain open.
- `e29a698` makes API read transfers abort on short, invalid or erroring writes, including public error envelopes, without appending another response or changing error classification. HEAD/304 and successful JSON remain unchanged. The short-write regression failed before the repair; independent source review and isolated full API races passed three times (1.752s), with vet. Mappings: `api-read`, `http-responses`, `testing-suite`.
- `e76a571`, `3eef163` and `c3c9ebd` add the shared authorized read-only generic boundary, named TemplateView and validated RedirectView constructors. Request metadata and scalar route parameters are frozen; GET/HEAD and opt-in authorized OPTIONS never implicitly mutate; current grants fence complete rendering and redirect emission. Template context has explicit precedence, private map-loader/config capture, method-free bounded data projection, a shared final context budget and engine-initialized locale before processors. Redirects support literal/named/dynamic sources, optional exact query forwarding, 301/302/Gone and twice-checked explicit external-target authority without trusting Host. Root/Admin/Data source reviews closed. Full HTTP races passed three times (initial 1.942s; final independent 2.357s); redirect fuzz passed 160,693 inputs; vets passed. Isolated projected-index boundary, template and redirect slices passed HTTP races (1.903s/1.791s/1.912s) with related URL/template/i18n races and vets. External-package routed examples plus the complete HTTP suite passed three times in their isolated slice (2.313s), with vet. Mappings: `http-generic`, `http-catalog`, `http-responses`, `http-routes`, `templates-render`. List/detail/edit/date views, general converter-value compatibility, browser proof and complete Django parity remain open. Hooks and providers are trusted cooperative Go code, not a sandbox; no application server was started. Whole-snapshot verification is pending for these additions.
- The immutable `ac6b4c4f30a664d812807eb787553baa7e5bdcc4` snapshot passes the complete seven-module race suite with required actual PostgreSQL/Redis services (native integration 182.428s; demo 4.847s), all six Admin JavaScript contracts (80.687ms), vet/build, and all six independently installed modules plus a fresh generated consumer with workspace overrides disabled. The isolated dependency file proxy retained fresh module caches and checksum verification; cleanup completed. This is the latest fully closed packaging-inclusive snapshot and includes the flatpage, lifecycle, health and response work below. Subsequent generic-view implementation is excluded. No network publication, deployment, browser review or complete Django/Celery parity is asserted.
- `ac6b4c4` freezes validated response headers, body buffers, original method and bounded conditional decisions before renderer/context/writer callbacks; rendering failure still takes precedence over conditional 304. Response.Write now rejects short or invalid over-count writes, preserving explicit writer errors, and Adapt aborts after committed headers without a second response. Root/Admin source reviews closed. Existing failures were reproduced before repair; full HTTP races passed three times (1.611s; independent 1.330s), both vets and diff checks passed. Mappings: `http-responses`, `http-generic`. No public error reclassification, new global response limits or change to render/conditional precedence was added. The complete snapshot gate above includes this repair.
- `1e94d38`, `95b1b9f` and `4d26857` add secret-free lifecycle snapshots and explicit role-specific live/startup/readiness SDK/HTTP probes. Startup success stays latched through drain; readiness checks current admission/lifetime independently of dependency availability. Selected concurrent probes share one short flight and bounded cache; canceled waiters do not cancel peers, stuck callbacks retain per-dependency/global capacity, and late completion cannot rewrite observed results. Only explicitly bypassable optional failures yield degraded readiness. Public JSON contains status/correlation only; separately configured diagnostics require current grants before evaluation and emission, followed by a fresh local drain fence. Root/Admin/Async source reviews closed. App races passed three times (1.903s; independent 1.512s); health races passed three times (1.967s; final independent 2.152s); actual PostgreSQL/Redis health races passed three times (1.761s/1.922s; independent 1.503s/1.675s), with all corresponding vets. Isolated projected-index checker and HTTP slices also passed core/provider races and vets (core 1.809s/1.797s, PostgreSQL 1.643s/1.718s, Redis 1.650s/1.686s). Mappings: `operations-health`, `core-bootstrap`, `core-lifecycle`, `http-responses`, `connectors-extend`. Only owned test client pools were closed; no server outage/restart was simulated. No orchestration, background monitor, deployment or full operations conformance is claimed; whole-snapshot verification is pending.
- The immutable `3a05b761ff3b6edefc0218b340a37babd825eb9d` flatpage snapshot passes the full seven-module race suite with required actual services (native integration 177.113s; demo 4.767s), six Admin JavaScript contracts (38.512ms), vet and build. Independent module/consumer verification has not been repeated for this snapshot, so the `2f8d79f` gate below remains the latest fully closed packaging checkpoint. Subsequent lifecycle/health work is excluded.
- `337621a`, `24a590a` and `3a05b76` add flatpage/site-through models and explicit migrations, bounded canonical paths, private named-template rendering, a granularly authorized transactional page/site replacement service, read-only repeatable-read lookup and GET/HEAD handlers. Site URL/pair uniqueness, page/membership equality fences, every retained/added/removed-site grant, callback/trigger rollback, cross-site content isolation, private-page concealment and final reader reauthorization have actual PostgreSQL regressions. Caller-known create-only identities permit explicit reconciliation after uncertain commit without an upsert; real committed data plus a synthetic lost acknowledgement returns zero Saved, while confirmed commit followed by cancellation retains success. Root/Admin/Async source reviews closed; full core races passed three times (1.471s; independent 1.698s), actual PostgreSQL races passed three times (3.456s; independent 3.476s), path fuzz exercised 95,120 inputs, and vets passed. Each staged slice also passed an isolated projected-index race/vet gate (models 1.663s; store core/PostgreSQL 1.697s/2.301s; HTTP core/PostgreSQL 1.742s/2.213s). Mappings: `contrib-flatpages`, `data-builtin-models`, `http-routes`, `templates-render`, `orm-transactions`. This is not network-loss, browser or credential-provider proof. Admin forms, automatic cache invalidation, full Django middleware behavior and complete conformance remain open; URL equality is a domain invariant, not a composite database constraint. The whole-snapshot gate for this checkpoint is pending.
- The immutable `2f8d79fffe7213d2f19205980b5799ca64cf86aa` snapshot passes the full seven-module race suite with required actual PostgreSQL/Redis services (native integration 177.009s; demo 4.765s), all six Admin JavaScript contracts (41.258ms), vet/build, and all six independently installed modules plus a fresh generated consumer with workspace overrides disabled. The isolated dependency file proxy retained fresh module caches and checksum verification; cleanup completed. This supersedes `f9c209d` as the latest whole-snapshot gate and includes redirects and the terminal database-result repairs below. Subsequent flatpages work is excluded; no publication, deployment, browser review or complete parity is asserted.
- `2f8d79f` adds persisted site-bound redirects, explicit migrations/FK/composite uniqueness, bounded exact/slash/query-aware lookup, read-only repeatable-read local-chain validation and an authorized GET/HEAD route-miss handler. Empty targets produce Gone; external first hops require a separate explicit grant; cycles, malformed/ambiguous records, partial providers, cancellation and failures expose no partial target. Receiver/config/request snapshots and complete row/transaction cleanup are covered. Root/Admin/Async source reviews closed; complete redirect races repeated three times passed (1.750s; independent 1.568s), URI fuzzing exercised 12,195 inputs, actual PostgreSQL redirect/save regression runs passed three times (2.115s; independent 1.784s), and corresponding vets passed. Mappings: `contrib-redirects`, `data-builtin-models`, `http-routes`, `http-responses`. Application-view 404 interception, automatic Admin setup, remote-chain cycle proof and complete Django middleware conformance are not claimed. The complete snapshot gate above now covers this checkpoint.
- `a0530c5`, `9286d2f` and `c39db6e` repair terminal result handling in the PostgreSQL connector, `db.QueryRow` and ORM Save. A real deferred-FK autocommit regression previously returned nil while persisting zero rows. Query completion now checks Close/post-close errors; missing-row sentinels cannot mask cleanup failures. Save waits for statement completion before generated-field hydration, persisted state or post hooks, and rejects trigger-suppressed inserts while preserving UPDATE-miss and caller-transaction timing. Driver tests retain SQLSTATE/constraint identity without exposing raw provider details; byte buffers are detached without rewriting custom driver types. Root/Admin/Async reviews closed; full DB and ORM races passed repeatedly (independent 1.215s/1.700s), close-boundary races passed (independent 1.324s), actual PostgreSQL FK/trigger regressions passed repeatedly with the redirect tests, and all corresponding vets passed. Mappings: `postgres-execute`, `orm-save`, `orm-transactions`, `connectors-extend`. Injected transport failures are not real network-loss or rollback proof; no retry was added.
- The immutable `f9c209de82b20923b2a4eb07a6e669d83a4474ae` snapshot passes the full seven-module race suite with required actual PostgreSQL/Redis services (native integration 180.544s; demo 4.906s), all six Admin JavaScript contracts (50.184ms), vet/build, and all six independently installed modules plus a fresh generated consumer with workspace overrides disabled. The isolated dependency file proxy retained fresh module caches and checksum verification; cleanup completed. This supersedes `86bcdef` as the latest whole-snapshot gate and includes sitemap, windows, signal/save snapshots and route-miss handling. Subsequent redirect implementation is excluded; no network publication, browser review, deployment or complete parity is asserted.
- `dcecf8c` and `f9c209d` isolate only test-constructed Async/Core management projects from ambient `GOGO_` settings. Exact cleanup restores prior values, including present-empty variables, and preserves unrelated environment keys. Strict production configuration behavior is unchanged. Independent full management races repeated three times passed (Core 1.907s; Async 1.379s), root Core passed (1.648s), Async own passed (1.758s), and all corresponding vets/source reviews closed. Mappings: `testing-suite`, `tools-commands`, `models-legacy`, `async-monitor`. These fix the command-fixture failures in the otherwise native-passing `c301cc4` whole-snapshot attempt; that attempt itself is not counted as a complete passing gate.
- `c301cc4` adds immutable `Router.WithNotFound` registration and operation-local dispatch capture for explicit final route misses. Matched-view 404s, method denials, OPTIONS and ordinary matching/reversal remain unchanged; custom fallback HEAD responses suppress bodies and nested misses clear only route-match metadata. Independent probes reproduced converter-time handler replacement and inherited outer-match metadata before repair. Root URL/HTTP races repeated three times passed (1.691s/1.607s), independent fallback races passed (1.416s), both vets/diff checks passed and source review closed. Mappings: `http-routes`, `contrib-redirects`; this is redirect integration support, not a persisted redirect feature or full router conformance claim.
- `456108b` preserves completed signal results and receiver errors across observed cancellation, rejects invalid contexts safely, and freezes registrations before context callbacks. Ordinary and robust dispatch retain their distinct error behavior. Root/Admin reviews closed; full repeated races passed (1.547s), independent repeated dispatch races passed (1.376s), and vet/diff checks passed. `c877e79` separately freezes Store configuration, save-hook lists and UpdateFields before callbacks while retaining mutable save events and existing no-op, prepare/guard, panic and transaction timing. Full ORM races passed (1.462s), actual PostgreSQL save/transaction regressions repeated three times passed (2.370s), independent save races passed (1.270s), and both reviews/vets closed. Mappings: `core-signals`, `orm-save`; stable-ID built-in request/model/migration/test/task signal integrations remain open.
- `500ac63` adds typed row-preserving window annotations, all eleven initial ranking/distribution/value helpers, aggregate windows, partition/order clauses, bounded ROWS/RANGE frames and explicit exclusions. Windows use existing root/related scopes and typed decoding; unsupported placements or connector capabilities fail before SQL. Independent Root/Admin source reviews closed after fixing shared-tree work-budget bypasses and direct/indirect window aliases in JOIN endpoints. Actual PostgreSQL window tests repeated three times passed (1.976s); full compiler/ORM races passed (1.724s/1.553s), root compiler races passed (1.669s), and three-package vet/diff checks passed. Mappings: `orm-expressions`, `orm-select`, `orm-operation-catalog`, `postgres-execute`, `connectors-extend`. Grouped windows, window-output filtering, RANGE offsets and complete expression conformance remain open.
- `c6d1167` adds authorized sitemap URL sets and indexes, explicit-origin/path constraints, canonical language alternates, detached metadata and byte-accurate bounded splitting. Revisioned section providers are checked before reads and again before rendering or conditional replies; stale, private, cancelled and failed reads expose no partial XML. Root/Admin/Async reviews closed; complete sitemap races repeated three times passed (2.012s), actual PostgreSQL publication/revision/scope/overflow tests repeated three times passed (2.596s), independent boundary races passed (1.552s), URL fuzzing exercised 33,127 inputs and vet/diff checks passed. Mappings: `contrib-sitemap`, `http-responses`; cross-page reciprocal-language correctness, stable provider revision ownership and asynchronous publication remain caller/integration responsibilities, not inferred guarantees.
- The immutable `86bcdef50e8fe7c5477b05ca2acfcb41aab4be10` snapshot passed strict actual PostgreSQL/Redis integration (186.014s; demo 4.998s), six Admin JavaScript contracts (63.313ms), vet/build, and all six independent public modules plus a fresh generated consumer with workspace overrides disabled. The module check used the subsequently fixed `c420443` runner against unchanged snapshot product source; checksum verification and fresh dependency caches were retained through the explicit dependency file proxy. Completed test artifacts were removed successfully. This supersedes `20063d1` as the latest whole-snapshot gate, after disk capacity recovered. Later cleanup, sitemap, window and lifecycle fixes are excluded; no network publication or full parity is asserted.
- `86bcdef` adds read-only `inspectdb` source/command generation and generated-project registration. Portable unmanaged models preserve physical mappings and require explicit identity for keyless views; unsupported native types/constraints/index semantics are rejected, not guessed. Review fixes freeze options, enforce aggregate CLI bounds before resources and distinguish PostgreSQL PK key columns from INCLUDE columns. Root/Async source reviews closed; all PostgreSQL races passed (10.364s), independent catalog/inspectdb tests repeated three times passed (10.274s), management/codegen/DB/models/migrations/ORM races and vets passed, and source fuzz exercised 6,786 inputs. Actual catalog-to-source-to-typed-codegen-to-separately-compiled-client tests cover reads, writes and relations. Mappings: `models-legacy`, `tools-commands`, `tools-command-catalog`, `tools-project`, `postgres-schema`, `connectors-extend`; custom codec generation and exact baseline adoption remain open.
- `c420443` repairs isolated module-test cleanup: owned read-only module directories regain owner permissions without following symlinks, and cleanup errors are reported. Root repeated races passed (1.677s), vet passed and independent Admin source review closed. Twelve completed Gogo module-test cache leftovers were removed; no project source, global downloaded dependencies or database records were removed. Mappings: `testing-suite`, `release-modules`. Disk-exhausted whole-snapshot attempts after `20063d1` are not counted as passing verification.
- `8cb9f38` adds bounded public RSS 2.0/Atom 1.0 rendering, explicit feed/item/enclosure grants, detached read-only metadata, configured-origin URL validation, custom XML formats and named-route HTTP integration. `5b97701` closes invalid Atom category IRIs and non-concrete enclosure media types, preserves RSS email-only authors, and adds actual PostgreSQL publication/withdrawal/deletion/overflow/denial/provider-failure tests. Root/Async/Admin reviews closed; final independent three-repeat syndication and PostgreSQL suites passed (1.444s/2.437s), root syndication/HTTP/URL suites passed (2.291s/3.707s/2.921s), and URL fuzzing exercised 978,567 inputs. Exact-body ETags reauthorize before 304, errors are no-store and late writes abort the stream. Mappings: `contrib-feeds`, `http-responses`; complete Django feed-helper and extension-format conformance remains open.
- `cf3ff91` adds the Site model with explicit migrations, trusted-host/explicit-ID resolution, detached request context, optional versioned namespaced positive caching and transaction-aware cache bypass. Root/Admin reviews closed; repeated unit races and actual PostgreSQL tests passed (independent 1.226s/1.671s), host fuzzing exercised 3,393,928 inputs, and vet/diff checks passed. Host selection is not tenant authorization; no automatic Admin registration, seed record or default site fallback is created. Mappings: `contrib-sites`, `data-builtin-models`; complete site-consumer integration and conformance remain open.
- The immutable `20063d1e25cf6e2166b91a3e2239d3dfea6e6833` snapshot passed strict actual PostgreSQL/Redis integration (189.564s; demo 11.558s), six Admin JavaScript contracts (47.730ms), vet/build, and all six independent public modules plus a fresh generated consumer with workspace overrides disabled. The explicitly configured dependency file proxy retained fresh module caches and checksum verification; this is not a network-publication test. This supersedes `9f9330e` as the latest whole-snapshot gate. Later fixes and working features are excluded.
- `20063d1` adds optional connector-neutral catalog inspection and a PostgreSQL read-only repeatable-read implementation, with exact raw metadata, explicit unsupported mappings, qualified system operations, bounded definitions/counts and no migration adoption. Root/Async reviews, broad data races/vet and actual PostgreSQL catalog tests pass. `c529137` subsequently closes scalar-query row-close failures with local bounded reading and permanent first/second-query regressions (independent three repetitions 1.554s; own two repetitions 1.878s). Mappings: `models-legacy`, `postgres-schema`, `connectors-extend`; source generation and the full legacy workflow remain in progress.
- `d05b166` adds bounded, snapshotted, escaped Admin search help with conditional ARIA linkage, without changing query or list-edit authority. Root/Async reviews closed, focused races repeated three times (1.837s; independent 1.491s), actual PostgreSQL Admin integration passed (41.273s; Admin 1.382s), and vet/diff checks passed. Mappings: `admin-options`, `admin-list`; no new browser-verification claim.
- `fa989da` snapshots every slice-backed security-header trust configuration at construction. Permanent pre-fix tests reproduced injected host/proxy policy changes; host/proxy/CORS mutation and concurrent configuration reuse are isolated after repair. Root security/HTTP/auth-view/session race suites repeated three times passed (1.598s/1.707s/2.204s/2.623s), independent Admin security races passed (1.395s), with vet/diff checks. Mappings: `security-headers`, `security-catalog`; other security policy catalog work remains open.
- `69d7328` adds bounded calendar-only formatting without changing existing date/time filters: preserve selected date fields, date-only ISO output, reject time tokens and resolve explicit midnight only when required. `b2b6e2f` adds all six approved humanize display helpers/filters with exact decimal and binary-float integer conversion, locale grouping, signed plural selection, literal fallback, deterministic relative clocks, request/explicit zones, escaped text and stable errors. Numeric and temporal independent reviews closed; final humanize/templates races passed (1.851s/2.224s), earlier full suites repeated three times, vet/diff checks and executable public examples passed. Root fuzzing exercised 681,370 numeric and 1,198,349 temporal inputs; independent temporal fuzz exercised 208,143. Mappings: `contrib-humanize`, `templates-catalog`, `templates-render`, `i18n-locale`; existing English date names, typed calendar-only model projection and complete localization conformance remain open.
- `cfa230a` adds optional top save controls through one shared Admin submit-row template, retaining one form/token set and existing permission, validation, transaction, audit and redirect behavior. Ordinary add/change and user change forms are covered; specialized credential forms remain unchanged. Root/Async independent reviews closed, focused races repeated three times (1.788s; independent 1.386s), and broad actual PostgreSQL Admin integration passed (39.473s; Admin 1.334s), with vet/diff checks. Mappings: `admin-options`, `admin-form`, `admin-save`; visual verification remains open.
- `f3502ef` implements bounded Admin show-all pagination with exact query-mode validation, a cap-plus-one read, count/identity consistency checks, preserved scope/filter/search/sort links and matching list-edit tokens. Root/Async review closed; focused races and repeated actual PostgreSQL tests passed (independent 4.425s), with vet. Mappings: `admin-options`, `admin-list`, `admin-actions`; this is not a cross-request snapshot or browser-conformance claim.
- `1fcec5b` preserves historical automatic many-to-many relations through an optional before/after schema-transition contract, reversed boundaries, detached defaults and exact bridge ownership. Native removal/replacement/reversal, explicit-through exclusion, transient relation handling and rollback/retry have actual PostgreSQL regressions; unrepresentable transient targets fail before DDL. Root/Admin reviews closed; full PostgreSQL race suite passed (10.224s), independent repeated regressions (2.176s), focused migrations/model/DB/ORM races and vet passed. Mappings: `migrations-detect`, `migrations-apply`, `migrations-reverse`, `postgres-schema`, `orm-relations`; full migration conformance remains open.
- `207e882` freezes GroupResult operation configuration/identity, bounds and detaches provider graphs/results, validates topology and stable layout, rechecks current grants while waiting/streaming, distinguishes missing retained output and preserves separately authorized revoke semantics. `69e5931` also snapshots single-task Result client values against callback-time replacement. Root/Admin/Data independent reviews closed, repeated boundary regressions and full Async/actual Redis races passed (GroupResult 29.023s/18.660s; Result follow-up 29.077s), with vet. Mappings: `async-results`, `async-group`, `async-cancel`, `async-canvas-catalog`; complete result/monitoring catalogs remain open.
- The immutable `9f9330edbd96a77400d56f831ffc0021b4039b86` snapshot passed strict actual PostgreSQL/Redis integration (186.517s; demo 12.844s), six Admin script contracts (48.187ms), vet/build, and independent workspace-disabled installation/tests for all six public modules plus a fresh generated consumer. Configured dependency caching retained checksum verification. This supersedes `7a70aeb` as the latest completed whole-snapshot gate and includes every checkpoint through explicit API DELETE. Subsequent working changes are not included; all results remain local, not release/deployment or parity evidence.
- `9f9330e` adds explicit API DELETE with a bounded freshly locked scoped graph, per-object/effect permissions, read-only graph policy, ordinary ORM hooks, same-transaction audit and final deleted/join/update/target checks. Registration snapshots structural dependencies; mixed operational errors cannot masquerade as completed denials. Strong If-Match, cookie CSRF, hidden dependency no-broadening, automatic joins, late mutation/cancellation, concurrent single audit and confirmed/unknown commit behavior have actual PostgreSQL regressions. Independent Admin review closed; final root API/ORM races repeated three times (4.377s/4.270s), PostgreSQL DELETE matrix repeated three times (4.647s) and vet pass. Mappings: `api-write`, `orm-delete`, `http-responses`; delete receipt replay is explicitly disabled and complete generic/API parity remains open.
- The immutable `7a70aebc9dc638b6893c90445ee9b212f02bab35` snapshot passed strict actual PostgreSQL/Redis integration (190.046s; demo 12.375s), all six Admin script contracts, vet/build, and fresh workspace-disabled installation/tests for all six public modules plus a generated consumer. Dependency checksum verification remained enabled. The newer `9f9330e` whole-snapshot gate above supersedes this result. None of these results establishes release/deployment or full Django/Celery parity.
- `92b2c71` adds request-local template timezone blocks/tags/filters with explicit allowlisted overrides, project-default restoration, detached temporal values and no global timezone mutation. `ad4aac3` implements bounded English Django-style date/time format tokens, named defaults, escaping, exact fractional/negative-epoch handling and signed historical offsets. Independent reviews, consumer races, vet and fuzzing pass (final date-format fuzz 62,889 executions). Mappings: `templates-render`, `templates-catalog`, `i18n-locale`; non-English date-name preferences and calendar-only typed projection remain open.
- `caf2393` adds guarded declared unique/check/conditional-unique constraint lifecycle, historical state, deterministic detection, rollback/reverse and physical catalog ownership validation. `804284f` orders native FK/O2O migration operations by deterministic dependencies while preserving model history and rename ordering; cycles and retained unique-key bindings require explicit staged operations. Actual PostgreSQL regressions reproduce missing target/uniqueness order, drift and rollback cases; independent Admin reviews and full focused data races/vet pass. Mappings: `models-schema`, `postgres-schema`, `migrations-detect`, `migrations-apply`, `migrations-reverse`; automatic-M2M historical removal, cyclic split-DDL and online migrations are separate open work.
- `bd4b4d7` adds validated Admin user-sort allowlists and stored-field mappings for computed columns, with exact query parsing, stable identity tie-breakers and single active accessible sort state. `04d079b` adds site/model/column empty-value display precedence, explicit empty strings, snapshotted configuration, bounded method-free emptiness checks and escaped list/readonly/inline rendering without extra relation reads. Root/Async reviews, repeated Admin races/vet and actual PostgreSQL Admin suites pass (empty display 38.742s). Mappings: `admin-options`, `admin-list`, `admin-form`; browser verification and complete Admin conformance remain open.
- `6bf5b93` validates scoped queue/local-worker inspection with frozen requested identity, detached bounded response metadata, rechecked grants and no partial provider/cancellation failures. `0a4e31e` binds single-task Result reads, waits, forget and revoke to one captured client/task identity, validates/detaches provider records and timestamp locations, preserves separate grants and distinguishes late confirmed mutation replies from possible-applied failures. Permanent exploits failed before repair; root/Admin reviews, repeated races, full Async/management and actual Redis gates pass (Result 25.178s/1.614s/18.684s). Mappings: `async-monitor`, `async-results`, `async-cancel`; GroupResult and complete monitoring/result catalogs remain open.
- `b641bc1` exposes `orm.CheckDeletionPlan` for coordinators needing fresh detached read-only graph checks without querying or mutating transaction state. Bounded preflight rejects protected/unsafe graphs; retained old views cannot alter later checks, and current-view mutation rejects authorization. Independent Admin review, repeated ORM races (1.403s) and vet pass. Mapping: `orm-delete`; callers still own locking, recollection, atomicity and public error boundaries.
- The immutable `ea78a9682559b90f00e4b7c39093e96fe1358da3` snapshot passed strict actual PostgreSQL/Redis integration (191.255s; demo 13.157s), all six Admin script contracts, vet and build. A normal retry also passed workspace-disabled installation/tests for all six public modules and a fresh generated consumer, using an explicitly configured cached-dependency file proxy without disabling checksum verification. This supersedes earlier dependency-proxy/checksum-server TLS failures, which were not counted as passes. The test helper honors configured dependency proxies behind the packaged-source proxy while retaining fresh module caches and disabled workspace overrides. These are local verification results, not release/deployment proof.
- `1f0a2e0` adds request-local DateTime/SplitDateTime form binding and rendering, unique wall-time resolution, explicit ambiguous/nonexistent-time rejection, UTC persistence and unchanged typed-fold identity. Tests cover half-hour/day/23-hour transitions, nanosecond-preserving form redisplay, readonly/disabled forged input, component validators and actual PostgreSQL model-form saves. Root/Data independent races, vet and fuzzing pass (root wall-time fuzz 885,233 executions; independent 1,173,940). Mappings: `i18n-locale`, `forms-bind`, `forms-catalog`, `forms-model`; complete date/time and localization conformance remains open.
- `90fedab` extends scoped ordinary FK/O2O choices to Admin inlines and fences parent plus every written inline after all authorization/scope callbacks. An initial or formset-row permission denial cannot widen during later model binding; late callbacks cannot retarget earlier child rows or frozen parent links. Non-PK targets, nullable/readonly/custom-widget modes and hidden-prior audit privacy have regression coverage. Root/Async independent review, Admin races/vet and final actual PostgreSQL Admin suite (40.506s) pass. Mappings: `admin-form`, `admin-save`; live browser verification remains blocked.
- `e82689f` implements named nonconcurrent index add/remove/replace/reorder/reverse with immutable migration snapshots, optional public backend capability, physical descriptor plus canonical catalog ownership, guarded renames and atomic rollback. Unsupported provider/concurrent/metadata combinations fail explicitly. Nonmanaged/nonstored schemas perform state-only transitions; indexed or unresolved-predicate column removal requires index removal first. Independent Admin review and actual PostgreSQL, migration, model, database and ORM races/vet pass. Mappings: `postgres-schema`, `migrations-detect`, `migrations-apply`, `migrations-reverse`, `models-schema`; constraint lifecycle and online migrations remain open.
- `7a70aeb` adds exact single-entry quarantine removal through an optional broker capability, explicit queue/record grant and complete inspected metadata. Atomic Redis comparison removes only the matching diagnostic entry, never live queue/task/result state. Confirmed, absent, conflicting, unattempted and unknown outcomes remain distinct across cancellation, lost acknowledgement and panic; wrapped/joined errors cannot assert no write. CLI input rejects missing, duplicate, case-variant and unknown identity keys before provider startup. Root independent review, repeated Async/management races, actual Redis races and vet pass. Mappings: `redis-records`, `async-monitor`; no replay or bulk/pattern deletion is included.
- Request/task locale selection now has immutable configured language/time-zone allowlists, bounded profile/cookie/header precedence, request-local overrides, safe failure and cache dimensions. Returned location pointers cannot mutate resolver-owned state. Typed application translation catalogs now compile into detached namespaced/context-aware maps, use explicit parent/fallback chains and exact signed-integer CLDR plural categories, and offer context-resolved lazy messages and development-only missing-key observers. Plain translations never execute templates or become trusted HTML. Independent review, race tests, template escaping and fuzzing pass (locale 793,152 and final catalog 888,329 executions). Catalog file/CLI compatibility, localized formatting and full i18n conformance remain open.
- Deletion SQL plans and compiler descriptors now stay private across authorization, scope and delete-hook callbacks. Detached graph/event views reject current-record, descriptor, state and nested-data mutations; stale views cannot retarget later objects. Tests reproduce and fix wrong-row deletion, scope-column rewrites, shared time-zone pointer mutation, named-pointer type loss and timestamp map-key collapse. Bounded opaque-value refusal applies only when callback views are required; no-callback custom-codec deletion remains supported. Root/Admin independent reviews and focused ORM/actual PostgreSQL races pass; the coherent integration snapshot above includes this repair.
- Ordinary Admin FK/O2O selectors now show only bounded, scoped, explicitly authorized choices and require exact target locks on writes. All callback-bearing authorization/scope work precedes final callback-free locked source/target checks, preventing late hook retargeting. Hidden prior identities are omitted from audit differences without claiming they were null. Independent root/Data review, full Admin races/vet, real-service Admin races (38.585s) and script contracts pass. The subsequent inline checkpoint is recorded above; live browser review remains blocked.
- Slug constructors now supply Django-like length/index defaults, and PostgreSQL maintains owned implicit DBIndex indexes with guarded create/rename/drop and catalog drift refusal. Migration state tracks external TargetFields/ThroughFields through logical renames, with rename-first comparison normalization preventing spurious relation alterations. Real catalog, shadow namespace, rollback, apply/reverse and cross-model relation tests pass; independent Admin/root reviews closed. The subsequent named-index checkpoint is recorded above; constraints and remaining model metadata remain open.
- API receipt maintenance now requires a separate explicit all-scope pruning grant, removes only bounded locked expired sealed receipts and never projects private request/response data. Concurrent batches, refreshed-expiry locks, denied/revoked authority, callback failures and confirmed/unknown outcomes have actual PostgreSQL regressions and independent review. This is an explicit maintenance call, not automatic background deletion or a new HTTP endpoint.
- Opt-in worker autoscaling grows bounded reservation loops under active load and retires only idle loops while accepted tasks drain. Shared hard capacity applies before consume/reclaim; malformed oversized provider batches remain pending, and late cancelled deliveries do not claim or acknowledge work. Independent source, repeated races and actual Redis tests pass. Scaling to zero, process recycling, global queue-depth fairness and complete worker conformance remain open.
- The immutable `6513621fa7964a8cee4ae50800f004fe220cc52a` snapshot passed workspace-disabled installation/tests for all six public modules and a fresh generated consumer, strict actual PostgreSQL/Redis integration (190.182s; demo 12.888s), vet and build. It supersedes the older `24fe303` full gate. Later checkpoints have focused checks and need another coherent full gate; these are local verification results, not a released or deployed product.
- Generic scoped resource PUT/PATCH now performs locked typed updates with model cleaning, explicit presence/default/null behavior, immutable identity and persisted-state guards around extension hooks, atomic audit and exact committed/unknown outcomes. Strong If-Match supports required preconditions; authorized detail responses can expose representation tags. Durable update receipts bind resource identity and conditions, replay under current authority, and do not rerun writes. Root and independently reviewed actual PostgreSQL tests cover same-version concurrent writers, rollback, eager nullable relations, policy mutation, cancellation, changed authority and lost acknowledgement. Mutation responses intentionally omit ETag after transformation. Generic delete, custom actions, reverse cursors and complete API conformance remain open.
- Scoped relational aggregates now support forward/reverse to-one joins and grouped many-to-many bridge/target joins, excluding hidden links before aggregate row multiplication. Reverse query names are distinct from relation accessor caches; typed non-primary keys and private automatic bridge provenance are preserved. PostgreSQL capability checks, compiler/clone cases and independent actual-service races cover these branches; windows, subqueries, inheritance and the complete ORM catalog remain open.
- Admin list editing is atomic across bounded scoped rows with validation, current field authority, optimistic conflict checks and redacted audit. Chrome confirmed successful synthetic product edits and rejected an oversized decimal batch; database readback confirmed exactly the successful effects/audits and no invalid-batch writes. Mobile inspection exposed offscreen accessible labels escaping the table cell. The committed containing-block CSS fix has source/layout regressions; its live post-fix recheck is pending because Chrome explicitly blocked automation while an extension panel was open. No automation bypass was attempted.
- Slug fields now default to the ASCII alphabet and opt into Unicode letters/numbers with WithAllowUnicode; that flag survives model/form/API derivation, schema cloning and generated/historical migration state. False is omitted from schema JSON to preserve earlier checksums. Forms retain explicit Strip behavior; model/API validation does not silently normalize. Independent review and cross-layer race tests pass. Django-like length/index constructor defaults and physical implicit index migrations are still pending this checkpoint.
- Admin PrepopulatedFields now provides escaped, prefix-aware add-form slug suggestions with ordered bounded sources, code-point length limits and optional Unicode mode. Existing/manual target values, disabled/readonly controls and malformed mappings fail closed; server validation remains authoritative. Six dependency-free Node VM cases, independent source/unit review and actual PostgreSQL Admin races (36.577s) pass. These script contracts do not claim live browser or inline prepopulation conformance.
- HTTP conditional GET/HEAD now validates every bounded If-None-Match header as a complete entity-tag list before returning 304. Malformed/oversized lists never partially match, and header presence suppresses date fallback. Source review, focused races and fuzzing pass. Async quarantine inspection now uses explicit per-queue/current per-record authority and payload-free records; complete monitoring/retention/failover catalogs remain open.
- Explicit HTTP create receipts now require one bounded operation key unless optional mode is deliberately selected. Stable actor/scope/model-contract identity binds method, host, path, parsed input and application Vary semantics; current add/object/row/relation/field authority is reapplied to both creation and replay. Eight concurrent requests produce one row/audit/receipt; body/path/host/Vary conflicts, current redaction/grant loss, hidden roots, late root/target mutations, unknown commits and after-commit failures have actual PostgreSQL tests. Independent review drove pre-transaction JSON marshaler panic containment and truthful pre-commit callback rollback reporting. Physical retention cleanup and update/delete receipt integration remain open.
- The immutable `24fe303bcc65311b43105139c3275283d968e667` snapshot passed isolated installation of all six public modules and the fresh generated consumer, strict PostgreSQL/Redis integration (180.831s), vet and build. This supersedes the earlier `8dd0f1a` full gate; subsequent commits have focused checks and still need a fresh coherent full gate. No module publication or deployed-product proof is implied.
- Chrome review of a disposable scoped staff fixture confirmed user identifier/direct-permission edits, group name/permission edits, disabled-password account creation and privileged password disabling. PostgreSQL metadata and four redacted audit rows independently confirmed the synthetic changes. The group editor was inspected at 390px width with no horizontal page overflow. UUID-heavy headings and cramped credential text drove scoped labels, an opt-in escaped actor label and shared form spacing regressions. Chrome explicitly blocked history navigation and password-reset POST; neither flow is claimed browser-verified and no bypass was attempted. The owned fixture/services were stopped and its schema removed; shared PostgreSQL was preserved.
- ModelForm now retains operational validation failures separately from user-correctable field errors; Admin parent/inline consumers return safe service failure instead of a misleading validation response. Independent source, race and actual PostgreSQL regressions cover joined provider/cancellation errors and inline rollback. This follows the earlier pure-validation classifier checkpoint.
- Implicit per-model aggregate annotations preserve distinct root identities, typed/null/JSON outputs, pagination and scoped eager reads; explicit grouping remains available. Automatic HAVING routing is a separate reviewed follow-up. Worker startup now renews its lease during task-start observation and confirms the fence before handler entry; the reproduced expired-lease entry fails before the fix and passes afterward. Beat calculations reject overflowing cron steps, unportable instants and nonadvancing custom calendars before occurrence construction, with real Redis recovery tests. These remain focused catalog slices, not full ORM/worker/scheduler conformance.
- Explicit typed JSON resource creation now runs model normalization after hooks/defaults/auto fields but before SQL encoding, then enforces readonly write authorization, scoped/locked relation targets, immutable inserted identity, safe representation and same-transaction audit. Callable UUID defaults, hidden/missing target equivalence, nested policy/audit mutation rollback, cookie CSRF and confirmed/unknown commit outcomes have actual PostgreSQL regressions. Independent review repaired pre-clean values reaching INSERT and foreign transaction errors substituting for the outer outcome. HTTP idempotency integration, update/delete and full write conformance remain pending.
- ORM Save has an explicit per-attempt mutable Prepare lifecycle without making ordinary Save automatically call FullClean. Provider errors and cancellation now survive model validation rather than becoming public field messages; bounded pure-validation classification protects API/HTTP responses from joined provider failures. Preparation, raw/update-fields/fallback/parent boundaries and single-encoding regressions pass. ModelForm operational-error propagation is a separate consumer follow-up in progress.
- Typed row annotations and explicit GroupBy/Having queries support scoped joins, filtered/distinct aggregates, typed output maps and counts. Reviews repaired transformed aggregate aliases reaching WHERE, alias cycles and output-identity mismatches; PostgreSQL also rejects names over its 63-byte identifier limit before silent truncation can collide. Implicit per-model grouping, automatic HAVING routing, windows and subquery conformance remain open.
- Default UserAdmin and GroupAdmin now include scoped group/direct-permission selectors with readonly/exclusion support, signed visible-selection snapshots and atomic redacted audit. Hidden links are retained server-side only under complete domain authority; submitted choices are rechecked after hooks. Custom stock grant overrides fail explicitly rather than being ignored. Independent code and PostgreSQL HTTP reviews pass, including unknown commits, stale edits and provider denial. Fresh Chrome review is pending.
- Async monitoring now offers explicit scoped event pages and lazy cancellable observation with stream and per-event current authorization. Queue/schedule, worker and progress observations are optional and lossy; relay, delayed and outbox observers run only after the corresponding source acknowledgement. Observer failures cannot substitute for durable outcomes. Independent race and actual PostgreSQL/Redis checks cover source-ack ordering, callback failures and payload redaction; dashboard and the full monitoring/control catalog remain open.
- Signed forward cursor resources now bind exact ordering/filter/page-size, stable tenant scope, verified actor/grants and resource/API version. Immutable public non-null ordering keys are required; hidden cursor keys fail closed, and current row scope is reapplied on every page. PostgreSQL tests cover earlier inserts, key precision, replay/tamper denial, and both directions for date/time/datetime/duration/decimal/IP positions (1.692s). Reverse navigation and snapshot isolation remain pending. Canonical relative links obey the same bounded query budget as requests.
- API durable operation receipts now have opt-in models/migrations, bounded same-transaction unique claims/locks, exact numeric request digests, current authorization and removal-only response redaction, atomic mutation/receipt rollback and documented expiry reuse. Initial/replayed JSON numeric bodies and object identities remain stable through JSONB. Independent review fixed foreign callback errors incorrectly implying this transaction committed; an explicit outer commit marker gates every response. Race/provider tests cover twelve concurrent callers, conflicts, tenant/user separation, revoked permission, current redaction, expiry, contended-lock timeout, failed serialization, callback panic and lost acknowledgement (latest focused API/actual-PG 1.504/1.858s). Resource HTTP write integration, retention maintenance and full API conformance remain pending.
- PostgreSQL commit-stage context/transport failures are now conservatively unknown, separate from ordinary query cancellation and definite server rollback responses. Injected database/sql provider tests verify classification without claiming a real network-loss experiment. This supports safe same-operation reconciliation in API/auth consumers.
- ORM typed Case/When expressions, exact typed-literal casts and scoped Query.Update now have compiler and PostgreSQL coverage. Update locks/transactions and matched-row counts cover atomic concurrent increments, scoped old-row selection, JSON/null and savepoint rollback; it intentionally bypasses instance save hooks. Shared provider field decoding now hydrates PostgreSQL fixed intervals as exact Go durations across typed records, MapRecord, Values, aggregate, bulk, refresh and eager paths; calendar-month/overflow intervals fail instead of rounding. Custom model codecs retain raw driver-value precedence. These are focused checkpoints, not full ORM conformance.
- Redis workflow repair now includes fixed-partition bounded inventory and explicit-authority coordinator cursors; gaps neither fabricate tasks nor starve later work when the caller retries the retained cursor. Strict lexicographic Results cleanup preserves active pins/tombstones and rejects legacy indexes/cursors without implicit migration. Shared intent transitions preflight all key types and metadata before any mutation. Actual Redis regression and independent review gates cover formerly partial Lua writes, exact revision agreement and cursor/callback safety; broad failover/fairness work remains open.
- Default UserAdmin now supports domain-backed identifier changes and explicit disabled-password creation in addition to flags and password changes. Core identity/group edits use exact delta authority, normalized identifiers, member auth-version invalidation and final persisted snapshot guards; nested extension changes roll back. CreateGroup likewise rechecks its exact grant-free snapshot. PostgreSQL tests cover self-session invalidation, audit rollback, unknown commit and later session-store failure. These new UI surfaces still require current Chrome review; GroupAdmin/grant selectors remain in implementation.
- Core configuration, registry lifecycle, authentication primitives, browser security, sessions, cache, named routing, structured responses and scaffold generation have focused race tests.
- The immutable `8dd0f1a` archive passed workspace-disabled local-proxy tests for all six public modules and a fresh consumer's project/app creation, typed model generation, migration generation, tests and compilation. This coherent rerun supersedes an earlier mixed-concurrent-edit failure. The same snapshot passed strict PostgreSQL/Redis integration (173.315s), vet and build. Newer checkpoints have focused verification and still require another full coherent snapshot gate. These checks verify local packages, not a published release.
- Disposable PostgreSQL integration covers save and validation branches, schema/migration history and rollback, scoped deletion and relational integrity, and scoped Admin writes with atomic audit.
- Disposable Redis integration covers cache/session compare-and-swap, rate limits, fenced task execution, delayed dispatch, durable workflow intents, group/chord results, periodic scheduling and recovery.
- A cross-module PostgreSQL/Redis test covers business/outbox rollback and eventual dispatch.
- Chrome desktop inspection has covered the Admin dashboard, product list and change form. A synthetic product save was verified afterward in PostgreSQL and its audit row. Browser history, inline, responsive and complete accessibility review remain open. Stale assets found in Chrome drove content-hashed asset URLs and cache-policy regressions.
- Independent reviews have identified and driven fixes for session cache headers, Admin deletion scope, audit snapshots, response-write failure handling and scaffold path confinement. Nested multi-alias transaction-context repair passes fake-backend and real PostgreSQL A/B/A rollback regressions; this does not close the complete transaction catalog audit.
- API serializers now have explicit model allowlists, nested/list/dictionary validation, PATCH presence rules, plain/scalar/file codecs, bounded parsers and negotiation. Read-only Resource handlers add named app routes, required request/object/field policies, token ceilings, mandatory root/eager query scopes, typed filter/search/order allowlists and bounded page/offset pagination. Counts and navigation require scope to encode all row visibility; count/page reads are not a consistent snapshot. Actual PostgreSQL tests cover scope, field-computation denial, stable PK ordering, safe detail absence, provider/cancellation classification and no partial responses. Review fixed partial email/URL operands incorrectly running model validators and decoder failures incorrectly becoming 404. Latest focused API/pagination/real-PG race gates pass (1.505/2.031/3.378s). Later checkpoints above add forward cursors and durable receipts; generic HTTP writes, custom actions and schema generation remain pending.
- Shared session/cookie/fallback flash storage has consume-once, plain-text/tag, size/level, privacy, tamper, failed-response and late-header tests. Independent review drove eager cookie-capacity checks and late Peek denial; Admin writes now emit generic success notices after persistence.
- Authenticated-session middleware reloads current grants and account version. Real PostgreSQL accounts and both PostgreSQL/Redis session providers pass the same credential HTTP workflow: CSRF, normalized identity/IP rate gates, exact password whitespace, generic denial, login rotation, safe redirects, logout, privilege-version invalidation and session-write failure. Independent reviews closed current-response CSRF rotation, account-switch flash leakage and mutable trusted-origin settings. Legacy hash catalogs and the complete authentication catalog remain open.
- Self-service password changes verify the current password, lock/recheck the account, preserve immutable validator claims and reject nested callback credential changes. Their HTTP and Admin adapters distinguish unchanged, committed and uncertain outcomes, never promote failed/unknown results, and require explicit current-session preservation. Real PostgreSQL plus both session providers cover exact data preservation, old-session/CSRF invalidation, provider-write failure and actual commit-then-unknown behavior. The independent concurrency review found stale rotation overwrote a competing session save; conditional versioned revocation now rejects that race before creating a replacement. Unsupported third-party session stores fail closed on existing-key rotation. Privileged UserAdmin and transactional security-audit completion remain open.
- Password resets have a separate opt-in migration, canonical purpose/identity-bound 256-bit secrets, digest-only token rows and one-hour expiry. Synchronous delivery resolves an explicitly verified recipient and sends Sensitive mail only after confirmed token commit; generic request acknowledgements and padded deadlines also cover resolver/provider failures and panics. Confirmation locks token then user, atomically consumes the token and replaces the credential, and never returns an authenticated principal. Actual PostgreSQL tests and independent review cover duplicate redemption, expiry, account-version invalidation, policy/callback rollback, after-commit failure and unknown commit. Request/confirmation HTTP forms now pass the full workflow with both PostgreSQL/Redis sessions, including token replay, policy retry, no automatic login and committed password plus session-cleanup failure. The content-hashed script moves a fragment token into the POST form and removes history; a Node VM DOM fixture verifies this behavior, not visual/browser conformance. Encrypted queued delivery, maintenance commands and the complete reset audit remain open.
- Built-in users, groups, permissions and join records have explicit migrations, authorization-before-write guards and persisted-state checks. Concurrent membership/grant tests observe actual PostgreSQL wait graphs and verify whole-transaction rollback on deadlock, explicit retry and version invalidation. Stable content types and scoped generic references have real PostgreSQL identity/version/concurrent synchronization tests. Custom-user relational swapping and the remaining generic-relation catalog are not complete.
- PostgreSQL sessions store only digests of canonical random bearer keys, bounded opaque values inside JSONB, versioned updates, expiry and revocation tombstones. Eight-writer CAS, exact large numbers, stale-create/save denial, bounded cleanup, version exhaustion and uncommitted-transaction rejection pass real PostgreSQL tests. Provider clocks govern expiry; idle-expiry policy and complete session-management commands remain open.
- Startup/shutdown has race-tested ownership, prevalidated resource selection, lifetime-aware readiness, deadline-bounded cleanup and redacted callback-panic handling. Non-cooperative Go callbacks can outlive deadlines; timeout does not prove they stopped. Command cleanup runs on callback failure and panic.
- Bulk create/update, named-constraint upsert, scoped scalar/many-to-many managers, automatic/explicit intermediary schemas and scoped join-row deletion have real PostgreSQL tests. Select-related uses joined SQL; prefetch batches related collections with scoped target queries, bounded finite self paths, nested/custom cache validation and parameter caps. Required unscoped joins participate in row locking; nullable/scoped branches preserve outer rows. Independent reviews and real PostgreSQL tests cover these slices; the complete eager/expression/lookup catalog remains open.
- Typed aggregate outputs now cover scoped sum/average/count/min/max, population/sample variance/deviation, DISTINCT, FILTER and explicit defaults with real PostgreSQL precision/empty-set tests. Sliced/grouped/locked aggregate sources fail explicitly until their subquery path exists. JSON model reads preserve exact numbers and explicit SQL/JSON null distinctions; equality nil matches JSON null, isnull matches SQL NULL, and native strings survive repeated model/form cleaning. Optional public dialects now support parameterized containment/key lookups and key/index paths, including scoped joined paths, text extraction, ordering and typed Values. JSON IN/range distinguishes native strings/null and compiles expression operands; empty predicates remove unused path bindings. Typed slice/map model fields survive cleaning, save and hydration without precision loss or partial assignment on error. Independent review and real PostgreSQL regressions cover these slices; advanced expressions and full lookup parity remain open.
- Admin many-to-many forms preserve hidden existing links without exposing them in choices, tokens or audit. Posted IDs are independently checked, including already-linked IDs rechecked after mutation hooks. Real PostgreSQL tests cover parent/relation/audit rollback and optimistic conflicts. Opt-in branded staff login/logout uses the shared account workflow, requires actual model grants and has PostgreSQL/Redis integration tests. New login, multi-select and autocomplete UI have not yet had Chrome visual review.
- Nested Async canvases persist ordered barriers and dispatch intents. Cancellation preserves active execution leases, and delayed-task replay retention includes the original schedule horizon. Task replacement retains the original logical result until its replacement graph settles; restart, nested workflows, callbacks, cancellation and delayed retention have real Redis tests. Oversized graph coordination settles dispatched children and releases pins without dropping their results. Worker/control/result-retention catalogs remain open.
- Flat task groups now expose ready/success/failure checks, non-propagating ordered outcomes and bounded streaming with per-yield authorization, partial-error handling, worker deadlock guards and authorized child-result recovery. Memory and real Redis tests pass; nested/chain/chord handles are not silently reinterpreted as flat groups. Process-tree cleanup now observes ordinary descendants exiting while the unreaped leader pins group identity; a bounded observation failure reports CHILD_FAILED. Repeated macOS race/stress tests and independent review support the checkpoint; occasional earlier safe cleanup failures are not fully explained. Linux cross-build is not runtime validation; escaped sessions/credentials require external supervision.
- Admin readonly many-to-many display uses a bounded scoped reader across flat forms, fieldsets and inlines; hidden targets/intermediaries never disclose labels, IDs or counts, and readonly forged POST fields do not write. Policy/provider errors and cancellation fail closed instead of silently hiding outages. Safe UserAdmin flags, account creation and privileged password/unusable-password editors use same-store Accounts and atomic redacted audit, with hashless projections, exact delta authority and protected deletion graphs. Review fixed mutable validation callbacks retargeting account identities and repeated identifier normalization. Actual PostgreSQL regressions cover rollback, add-only authority, committed/unknown acknowledgements and committed self-password change plus session-delete failure. Branded reset request/confirmation adapters and a local-only synthetic review demo are committed. Grant/group editors and complete UserAdmin behavior remain open; these new UI paths still require Chrome review.
- Worker presence has immutable public runner instances independent of secret ownership leases. Exact-instance remote shutdown now has bounded Memory/Redis transport, immutable request IDs, per-worker/queue authorization, partial/unknown submission and reply outcomes, restart protection and accepted-work drain. Accepted replies never assert process exit. Independent review found and fixed a malformed-time JSON-clone panic; source review and real Redis race tests cover the integrated SDK/runner flow. Remote control is explicitly opt-in, and no unapproved CLI verb was added. Complete worker scaling/recycling/recovery catalogs remain open.
- Exact-workflow reconciliation repairs missing barrier completions only from matching authoritative terminal task records. It reuses ordinary graph CAS/successor intents, preserves active pins/outcomes, never reruns or fabricates absent tasks, and reports source gaps. Bounded child cursors avoid starvation; per-workflow/task reconciliation authority is rechecked. Independent memory/actual Redis races pass (1.396/1.846s), with concurrent chord repair and cancellation regressions. Later checkpoints above add partition inventory/background coordination; full Redis failover conformance remains open.
- Shared credential-page renderers now drop partial error output and sanitize callback panics across login, password change and both reset forms; cancellation is checked before/after rendering. Independent Auth views/Admin races pass. Custom policy wrappers preserve explicit unconstrained anonymous rules while default ModelPolicy and invalid constrained identities still deny; no token ceiling is widened.
- Token scope ceilings are private, cloned and non-widening across principal contexts, ModelPolicy and custom policy wrappers. Account services enforce mapped model-action ceilings before their exact-delta authorizers; unknown actions deny constrained identities. Session loading, Login and RefreshLogin reject token-to-cookie promotion, and constrained principals cannot lose their ceiling through ordinary JSON. Opaque API tokens have independent models/migrations, digest-only secrets, explicit 24h default expiry, current grant/version checks and authorized issuance/revocation. Bearer HTTP fails closed on malformed/duplicate credentials, provider outage or panic without cookie fallback. Actual PostgreSQL tests and independent review cover concurrent/idempotent revocation, hook and authority rollback, expiry and committed/unknown outcomes without returning unconfirmed secrets. Review fixed callback-only no-op revocation reporting. Full token/JWT/OIDC and audit parity are not complete.
- Core mail now has bounded immutable MIME/envelopes, verified TLS SMTP, bounded pooling and per-recipient accepted/rejected/unknown/simulated outcomes without blind retries. Sensitive messages reject ordinary JSON, console and file output; all-verb formatter regressions protect routine rendering. Memory, console, private root-confined quota-limited file and dummy development backends plus configured administrative recipient helpers have focused and independent tests. Only synthetic local SMTP was contacted. SMTPUTF8, the complete SMTP authentication/transport catalog and encrypted durable delivery remain open.
- Shared flash storage now removes an exhausted queue without creating a blank session after logout. Newly queued anonymous notices still persist deliberately; confirmed retained-login password changes use a generic Admin notice. Session/fallback consume, failed-response and real PostgreSQL/Redis Admin logout regressions pass.
- Redis delayed/periodic/workflow intent payloads now remain opaque through Lua, and fences/revisions use exact decimal counters. Real Redis regressions cover object key order, values beyond 2^53, counter exhaustion and batch preflight before mutation. Cache increment returns exact Redis integer text. Earlier unversioned internal records fail closed; there is no automatic data migration. Independent review caught partial same-partition lease mutation on later-item error; fixed by whole-batch preflight. Separate Cluster partitions are not one atomic transaction.
- The strict cross-module integration gate passed against disposable supported PostgreSQL and Redis, followed by workspace vet and build checks. This is local conformance evidence, not deployment or exhaustive compatibility evidence.

## Verification entry points

- `make test`: Node.js script contracts and race tests for all workspace modules, including integration tests; unavailable optional local service binaries may cause explicit skips.
- `make test-js`: contributor-only Admin script contracts with Node.js built-ins; no browser, npm dependencies or public Go module runtime dependency.
- `make test-modules`: isolated public-module and fresh-consumer verification with workspace overrides disabled.
- `make test-integration`: require PostgreSQL 16+ tooling and Redis 7.2+ tooling on PATH (or explicitly configured fixture binary directories); tests allocate disposable local services.
- `make vet`: static checks for all workspace modules.

Service availability and skipped tests must always be reported separately from a pass. Neither a successful command nor this ledger marks AgentFlow implemented. Final completion requires the exact implementation audit and completion command.

## Verification interruption and recovery

Broader verification initially failed with `no space left on device`, even with package parallelism limited to one, at approximately 158 MiB free. Only verified Gogo test leftovers and two unused generated demo binaries were removed; the active demo, database and unrelated user files were preserved. Host disk capacity subsequently recovered without further cleanup by this task, allowing the complete `86bcdef` and then `f9c209d` gates above to pass. Builds remain serialized. Later product additions still need their own verification; these gates do not substitute for the 152-task AgentFlow completion audit.

Disk exhaustion recurred after the fully passing `e9e1d81` snapshot. Only verified disposable Gogo source/test archives from closed sessions were removed; their inputs remain in Git or the workspace. The active Admin demo, PostgreSQL fixture, unrelated files and shared caches were preserved. This recovered enough space for the scope checkpoint and full API schema race suite, but two mutational-fuzz build attempts still failed with `no space left on device`, including a race-enabled retry after another archive cleanup. Vet passed; the last cleanup left approximately 271 MiB free. Additional implementation requiring compilation, mutational fuzzing and the next whole-snapshot/packaging gate need more host disk capacity. No remaining archive or cache cleanup is silently authorized, and the approved architecture is still not marked implemented.

Host capacity subsequently recovered to approximately 34 GiB without further cleanup by this task. The serializer fuzz campaign and focused Async repair verification above now pass; compilation is no longer blocked by the earlier disk condition. The new whole-snapshot/packaging gate remains pending.

## Open task coverage

| Task | Approved feature | Current stage |
| --- | --- | --- |
| `map-repository` | Repository roots and independent Go modules | Partial code; full conformance pending |
| `map-scope` | Scope, compatibility and design decisions | Pending implementation or audit |
| `map-products` | Products, packages and runtime roles | Partial code; full conformance pending |
| `runtime-defaults` | Proposed defaults and environment requirements | Partial code; full conformance pending |
| `core-bootstrap` | Start an application and freeze its registry | Partial code; full conformance pending |
| `core-checks` | Run system and deployment checks | Partial code; full conformance pending |
| `core-lifecycle` | Drain requests and shut down resources | Partial code; full conformance pending |
| `core-settings` | Load configuration and validate the build target | Partial code; full conformance pending |
| `core-signals` | Dispatch typed lifecycle and model signals | Partial code; full conformance pending |
| `core-typed-errors` | Translate errors at the public boundary | Partial code; full conformance pending |
| `models-field-catalog` | Model fields, options and exact storage intent | Partial code; full conformance pending |
| `models-inheritance` | Resolve model composition and polymorphic identity | Pending implementation or audit |
| `models-legacy` | Inspect an existing database and map legacy tables | Partial code; full conformance pending |
| `models-schema` | Declare a model and generate typed accessors | Partial code; full conformance pending |
| `models-validate` | Validate an instance before persistence | Partial code; full conformance pending |
| `orm-bulk` | Create, update and upsert batches | Partial code; full conformance pending |
| `orm-cardinality` | Handle missing, singular and multiple rows | Partial code; full conformance pending |
| `orm-delete` | Collect and delete related objects safely | Partial code; full conformance pending |
| `orm-eager` | Load joins and collections without hidden N+1 queries | Partial code; full conformance pending |
| `orm-expressions` | Evaluate aggregates, expressions and database functions | Partial code; full conformance pending |
| `orm-locking` | Coordinate concurrent writers | Partial code; full conformance pending |
| `orm-native` | Use explicit native SQL and inspect query performance | Partial code; full conformance pending |
| `orm-operation-catalog` | Query, expression and lookup inventory | Partial code; full conformance pending |
| `orm-relations` | Read and mutate related objects | Partial code; full conformance pending |
| `orm-routing` | Route reads, writes and migrations to database aliases | Pending implementation or audit |
| `orm-save` | Insert or update a model with transaction hooks | Partial code; full conformance pending |
| `orm-select` | Build, execute and materialize a query | Partial code; full conformance pending |
| `orm-transactions` | Commit, nest or roll back a transaction | Partial code; full conformance pending |
| `orm-upsert` | Resolve create-or-update races safely | Partial code; full conformance pending |
| `postgres-connect` | Open and manage a PostgreSQL connection alias | Partial code; full conformance pending |
| `postgres-execute` | Compile PostgreSQL SQL and execute it safely | Partial code; full conformance pending |
| `postgres-extensions` | Use PostgreSQL-specific model and query features | Partial code; full conformance pending |
| `postgres-schema` | Execute schema operations and introspect results | Partial code; full conformance pending |
| `migrations-apply` | Apply migrations with locking and durable history | Partial code; full conformance pending |
| `migrations-data` | Run a historical data migration | Partial code; full conformance pending |
| `migrations-detect` | Detect model changes and write a migration | Partial code; full conformance pending |
| `migrations-online` | Expand, backfill and contract an online schema | Pending implementation or audit |
| `migrations-operations` | Migration operation catalog | Partial code; full conformance pending |
| `migrations-reverse` | Preview, reverse or explicitly fake a migration | Partial code; full conformance pending |
| `migrations-squash` | Squash and merge migration history | Pending implementation or audit |
| `redis-async-adapter` | Connect Async contracts to Redis storage roles | Partial code; full conformance pending |
| `redis-atomic` | Run atomic Redis operations with cluster-safe keys | Partial code; full conformance pending |
| `redis-connect` | Open Redis connections by workload role | Partial code; full conformance pending |
| `redis-reconcile` | Reconcile lost acknowledgements and Redis failover | Partial code; full conformance pending |
| `redis-records` | Redis keys, partitions and retention | Partial code; full conformance pending |
| `security-catalog` | Identity, session and security options | Partial code; full conformance pending |
| `security-csrf` | Validate CSRF and origin for cookie-authenticated writes | Partial code; full conformance pending |
| `security-headers` | Apply host, proxy, CORS and browser security policies | Partial code; full conformance pending |
| `security-signing` | Sign and verify time-bounded values | Partial code; full conformance pending |
| `auth-login` | Authenticate and create a session | Partial code; full conformance pending |
| `auth-password-change` | Change a password or privileged user grants | Partial code; full conformance pending |
| `auth-password-reset` | Reset a password without leaking account existence | Partial code; full conformance pending |
| `auth-password-reset-confirm` | Consume a reset token and replace the password | Partial code; full conformance pending |
| `auth-permissions` | Check action, object and field permissions | Partial code; full conformance pending |
| `auth-tokens` | Authenticate a bearer token and rotate credentials | Partial code; full conformance pending |
| `sessions-request` | Load, rotate, save and revoke sessions | Partial code; full conformance pending |
| `http-catalog` | HTTP, middleware and view building blocks | Partial code; full conformance pending |
| `http-generic` | Serve generic list, detail, edit and date views | Partial code; local-scalar read/date/Create/Update checkpoints verified; complete edit/delete/relation/date conformance pending |
| `http-request` | Receive a request and send a response | Partial code; full conformance pending |
| `http-responses` | Construct JSON, redirects, files and conditional responses | Partial code; full conformance pending |
| `http-routes` | Register, resolve and reverse named routes | Partial code; full conformance pending |
| `http-streaming` | Stream responses and reconnect SSE clients | Partial code; full conformance pending |
| `http-websocket` | Authorize and process WebSocket messages | Typed authorized JSON transport and explicit lifecycle checkpoint verified; complete conformance pending |
| `forms-bind` | Bind and validate a form | Partial code; full conformance pending |
| `forms-catalog` | Forms, formsets, fields and widgets | Partial code; full conformance pending |
| `forms-formsets` | Validate and save a formset or inline formset | Partial code; full conformance pending |
| `forms-model` | Save model forms without mass assignment | Partial code; full conformance pending |
| `templates-catalog` | Template engine, tags, filters and extension points | Partial code; full conformance pending |
| `templates-render` | Load, inherit and render a template safely | Partial code; full conformance pending |
| `api-catalog` | REST API feature and serializer catalog | Partial code; full conformance pending |
| `api-idempotency` | Claim and replay an idempotent mutation safely | Partial code; full conformance pending |
| `api-pagination` | Filter, search, order and paginate without scope leaks | Partial code; full conformance pending |
| `api-read` | Serve scoped resource lists and details | Partial code; full conformance pending |
| `api-schema` | Generate OpenAPI and typed clients | Partial output schemas, bound read OpenAPI and opt-in command; input/write and client workflow pending |
| `api-serializers` | Parse, validate and serialize typed resources | Partial code; full conformance pending |
| `api-write` | Create, update or delete a resource | Partial code; full conformance pending |
| `admin-access` | Authenticate staff and render the Admin index | Partial code; full conformance pending |
| `admin-actions` | Execute a custom action or bulk list edit | Partial code; full conformance pending |
| `admin-autocomplete` | Search related objects and select permitted IDs | Partial code; full conformance pending |
| `admin-delete` | Preview and confirm scoped deletion | Partial code; full conformance pending |
| `admin-form` | Render create, change and read-only forms | Partial code; full conformance pending |
| `admin-history` | Read history and customize Admin behavior | Partial code; full conformance pending |
| `admin-list` | Search, filter and paginate an Admin change list | Partial code; full conformance pending |
| `admin-options` | Admin configuration and hook inventory | Partial code; full conformance pending |
| `admin-register` | Register models and publish an Admin site | Partial code; full conformance pending |
| `admin-save` | Save parent, inlines and audit in one transaction | Partial code; full conformance pending |
| `cache-access` | Read through cache and invalidate after a write | Partial code; full conformance pending |
| `cache-invalidate` | Invalidate derived cache entries after mutation | Exact non-durable OnCommit checkpoint verified; durable/versioned invalidation and full conformance pending |
| `ratelimit-consume` | Enforce a distributed request or task quota | Partial code; full conformance pending |
| `files-direct` | Verify direct multipart uploads through a future storage adapter | Pending implementation or audit |
| `files-download` | Authorize download, deletion and signed access | Authorized owner-linked Open and private conditional/range HTTP streaming verified; deletion and signed access pending |
| `files-upload` | Receive, validate and persist an upload | Local Storage and prevalidated owner/metadata binding verified; raw upload and complete lifecycle pending |
| `static-collect` | Collect, fingerprint and serve static assets | Explicit source/hashed-manifest/command/template/debug checkpoint verified; complete post-processing/scaffold conformance pending |
| `email-send` | Build and send a validated email | Partial code; full conformance pending |
| `email-sensitive-delivery` | Deliver encrypted single-use reset mail | Pending implementation or audit |
| `messages-flash` | Store and consume one-time user messages | Partial code; full conformance pending |
| `i18n-locale` | Resolve locale, translate and format values | Partial code; full conformance pending |
| `fixtures-data` | Export and import structured fixtures | Scoped JSON/JSONL export, non-auto-key atomic import/dry-run and explicit CLI verified; automatic keys, relations, natural keys and format extensions pending |
| `contrib-admindocs` | Generate developer-facing model and route reference | Pending implementation or audit |
| `contrib-contenttypes` | Resolve model identities and generic relations | Partial code; full conformance pending |
| `contrib-feeds` | Generate RSS or Atom syndication feeds | Partial code; full conformance pending |
| `contrib-flatpages` | Serve a registered flat page | Partial code; full conformance pending |
| `contrib-gis-import` | Import, transform and export spatial datasets | Pending implementation or audit |
| `contrib-gis-query` | Declare and query spatial data through PostGIS | Pending implementation or audit |
| `contrib-humanize` | Format human-readable display values | Partial code; full conformance pending |
| `contrib-redirects` | Apply a site-bound redirect after route miss | Partial code; full conformance pending |
| `contrib-sitemap` | Generate sitemap indexes and bounded sitemap pages | Partial code; full conformance pending |
| `contrib-sites` | Resolve the current site and scope site-owned content | Partial code; full conformance pending |
| `async-beat` | Evaluate periodic schedules and dispatch one occurrence | Partial code; full conformance pending |
| `async-callbacks` | Dispatch callbacks, errbacks and task replacement | Partial code; full conformance pending |
| `async-cancel` | Revoke pending work or cooperatively cancel execution | Partial code; full conformance pending |
| `async-canvas-catalog` | Canvas operations, result composition and scheduling options | Partial code; full conformance pending |
| `async-chain` | Execute a chain and stop on failure | Partial code; full conformance pending |
| `async-chord` | Complete a chord barrier and dispatch its callback | Partial code; full conformance pending |
| `async-contracts` | Broker, result, scheduler and workflow adapter contracts | Partial code; full conformance pending |
| `async-delayed` | Move delayed and retried tasks into runnable queues | Partial code; full conformance pending |
| `async-group` | Fan out a group and collect ordered outcomes | Partial code; full conformance pending |
| `async-intent-relay` | Relay durable task and workflow intents across stores | Partial code; full conformance pending |
| `async-limits` | Apply concurrency, rate and hard time limits | Partial code; full conformance pending |
| `async-map` | Execute map, starmap and chunks | Partial code; full conformance pending |
| `async-monitor` | Inspect workers, task events and control operations | Partial code; full conformance pending |
| `async-outbox` | Commit business data and reliably dispatch a task | Partial code; full conformance pending |
| `async-publish` | Publish a task with routing and scheduling metadata | Partial code; full conformance pending |
| `async-reclaim` | Recover after worker crash or lease loss | Partial code; full conformance pending |
| `async-register` | Declare tasks and validate worker compatibility | Partial code; full conformance pending |
| `async-results` | Read task results, progress and retention state | Partial code; full conformance pending |
| `async-retry` | Retry transient failures or retain terminal failures | Partial code; full conformance pending |
| `async-signatures` | Compose immutable task signatures | Partial code; full conformance pending |
| `async-states` | Task state machine and durability boundaries | Partial code; full conformance pending |
| `async-task-contract` | Task declaration, dispatch options and wire envelope | Partial code; full conformance pending |
| `async-testing` | Test tasks and workflows deterministically | Partial code; full conformance pending |
| `async-worker` | Claim, execute, persist and acknowledge a task | Partial code; full conformance pending |
| `tools-app` | Generate and register a reusable app | Partial code; full conformance pending |
| `tools-command-catalog` | Central CLI commands and their side effects | Partial code; full conformance pending |
| `tools-commands` | Resolve and run custom management commands | Partial code; full conformance pending |
| `tools-project` | Generate a client project | Partial code; full conformance pending |
| `tools-reload` | Reload the development server safely | Pending implementation or audit |
| `operations-deploy` | Build and deploy web, worker and scheduler roles | Pending implementation or audit |
| `operations-health` | Report startup, readiness and liveness independently | Partial code; full conformance pending |
| `operations-restore` | Back up and restore data with consistency checks | Pending implementation or audit |
| `operations-scale` | Scale web, workers and storage independently | Pending implementation or audit |
| `operations-telemetry` | Trace requests, queries and tasks without leaking secrets | Pending implementation or audit |
| `testing-contract-matrix` | Required verification before any release claim | Partial code; full conformance pending |
| `testing-suite` | Run unit, integration and generated-client tests | Partial code; full conformance pending |
| `release-modules` | Publish compatible framework modules | Partial code; full conformance pending |
| `map-client-journey` | Developer journey: app, API, Admin and background workflow | Pending implementation or audit |
| `map-client-layout` | Generated client project and daily developer entry points | Partial code; full conformance pending |
| `data-builtin-models` | Built-in PostgreSQL records and relational constraints | Partial code; full conformance pending |
| `connectors-extend` | Add a future connector without changing application APIs | Partial code; full conformance pending |
| `services-catalog` | Shared service methods and provider contracts | Private local Storage contract/provider checkpoint verified; complete shared service catalog pending |
| `reference-feature-index` | Every feature and its flow | Pending implementation or audit |
| `review-coverage` | Feature coverage and deliberate compatibility boundaries | Pending implementation or audit |

## Cross-cutting release gaps

The complete serializer/API/authentication-account flows, remaining ORM/field/migration catalogs, Admin customization and inline combinations, nested Async canvas/control/reconciliation, shared services and contrib packages, operations, deployment examples, reference documentation, and full conformance matrix still require work. Independently audited production readiness, exhaustive Django/Celery compatibility, package publication and deployment have not been established.
