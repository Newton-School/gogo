package {{.AppName}}

import "github.com/Newton-School/gogo/auth"

const (
	ViewItemPermission   = AppLabel + ".view_item"
	ChangeItemPermission = AppLabel + ".change_item"
)

func CanViewItem(user auth.User) bool {
	return auth.HasPerm(user, ViewItemPermission)
}
