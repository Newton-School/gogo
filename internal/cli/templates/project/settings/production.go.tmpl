package settings

import "github.com/Newton-School/gogo/conf"

func Production() conf.Settings {
	settings := Base()
	settings.Env = "production"
	settings.Debug = false
	settings.SessionCookieSecure = true
	settings.CSRFCookieSecure = true
	settings.HTTPSEnabled = true
	return settings
}
