# Gogo Package And Feature Index

Use this rule as the first stop when an AI agent needs to discover which Gogo
package, CLI command, or generated project file owns a feature. This project
does not assume any external Codex memory. The local `.agent/rules/gogo/*`
files and public Gogo docs are the source of truth for framework usage.

Framework module: `github.com/Newton-School/gogo`.

Never import `github.com/Newton-School/gogo/internal` from this client project.
Use public packages only. When exact symbols or options are needed, run:

```bash
go doc github.com/Newton-School/gogo/<package>
```

## Public Package Map

| Import path | Feature ownership |
| --- | --- |
| `github.com/Newton-School/gogo` | framework entrypoint and public module |
| `github.com/Newton-School/gogo/app` | app configs, registry, discovery resources, lifecycle, and app management commands |
| `github.com/Newton-School/gogo/management` | project-local management entrypoint, project commands, checks, settings, routes, models, and queue wiring |
| `github.com/Newton-School/gogo/conf` | settings defaults, environment loading, validation, and deploy-safe configuration |
| `github.com/Newton-School/gogo/checks` | system checks, deploy checks, and contrib checks |
| `github.com/Newton-School/gogo/http` | routers, URL patterns, reversing, request/response helpers, middleware, decorators, redirects, generic views, static mounting, and server startup |
| `github.com/Newton-School/gogo/security` | CSRF, signing, security headers, and security middleware |
| `github.com/Newton-School/gogo/sessions` | session data, stores, cookie sessions, and session middleware |
| `github.com/Newton-School/gogo/health` | health check registration and health responses |
| `github.com/Newton-School/gogo/observability` | structured logging, metrics, and tracing hooks |
| `github.com/Newton-School/gogo/models` | model metadata, instances, registries, inheritance metadata, lifecycle state, indexes, permissions, and validation hooks |
| `github.com/Newton-School/gogo/models/fields` | scalar, numeric, text, boolean, temporal, binary, JSON, file, network, generated, relation, choice, PostgreSQL, and GIS field metadata |
| `github.com/Newton-School/gogo/models/constraints` | index metadata, unique constraints, check constraints, and naming helpers |
| `github.com/Newton-School/gogo/models/hooks` | model lifecycle hooks |
| `github.com/Newton-School/gogo/models/validation` | structured validation errors and validators |
| `github.com/Newton-School/gogo/orm` | database config, query state, querysets, managers, expressions, functions, lookups, aggregates, joins, prefetch plans, raw SQL, transactions, routers, and compilers |
| `github.com/Newton-School/gogo/orm/dialects` | dialect interfaces and shared dialect features |
| `github.com/Newton-School/gogo/orm/dialects/postgres` | PostgreSQL SQL compilation and feature flags |
| `github.com/Newton-School/gogo/orm/dialects/sqlite` | SQLite SQL compilation and feature flags |
| `github.com/Newton-School/gogo/migrations` | migration graph, loader, writer, executor, autodetector, recorder, state, safety checks, and migration metadata |
| `github.com/Newton-School/gogo/migrations/operations` | model, field, index, constraint, SQL, and data migration operations |
| `github.com/Newton-School/gogo/admin` | admin sites, registries, session/staff permission policies, model admin options, change lists, change forms, delete confirmation, history, actions, filters, search, inlines, widgets, assets, templates, URLs, and permissions |
| `github.com/Newton-School/gogo/api` | API requests/responses, serializers, model serializers, fields, validation, views, viewsets, routers, parsers, renderers, auth, permissions, throttling, filters, pagination, uploads, metadata, versioning, and OpenAPI |
| `github.com/Newton-School/gogo/auth` | users, groups, permissions, content types, authentication, memory/file user stores, password hashing, auth forms, password reset tokens, session middleware, decorators, admin registration, and auth migrations |
| `github.com/Newton-School/gogo/auth/migrations` | built-in auth migration metadata |
| `github.com/Newton-School/gogo/forms` | forms, widgets, form fields, formsets, model forms, validation, and structured form errors |
| `github.com/Newton-School/gogo/templates` | template engine, loaders, context processors, tags, filters, safe strings, and Django-compatible helpers |
| `github.com/Newton-School/gogo/static` | static file discovery, duplicate detection, hashed names, manifests, and collectstatic output |
| `github.com/Newton-School/gogo/files` | storage backends, upload objects, upload handlers, filename normalization, traversal protection, and overwrite avoidance |
| `github.com/Newton-School/gogo/messages` | request messages, storage, and message middleware |
| `github.com/Newton-School/gogo/i18n` | language and timezone helpers |
| `github.com/Newton-School/gogo/signals` | signal declaration, dispatch, and receivers |
| `github.com/Newton-School/gogo/queue` | queue app, task registration, URL runtime factories, signatures, envelopes, workers, beat, schedules, retries, rate limits, routing, pools, results, serialization, security, inspection, events, and admin metadata |
| `github.com/Newton-School/gogo/queue/brokers` | broker interfaces and shared broker types |
| `github.com/Newton-School/gogo/queue/brokers/redis` | real Redis broker for `redis://` and `rediss://` queue URLs |
| `github.com/Newton-School/gogo/queue/brokers/rabbitmq` | RabbitMQ route planning helpers; AMQP runtime URLs are unsupported until a real transport is registered |
| `github.com/Newton-School/gogo/queue/backends` | result backend interfaces |
| `github.com/Newton-School/gogo/queue/backends/redis` | real Redis result backend for `redis://` and `rediss://` result URLs |
| `github.com/Newton-School/gogo/queue/backends/sql` | SQL result backend implementation; URL factory support must be registered before use |
| `github.com/Newton-School/gogo/queue/schedulers/redis` | real Redis beat schedule store for `redis://` and `rediss://` schedule URLs |
| `github.com/Newton-School/gogo/queue/canvas` | chains, groups, chords, chunks, maps, and workflow serialization |
| `github.com/Newton-School/gogo/cache` | local, dummy, file, database, Redis, and memcached-style cache APIs |
| `github.com/Newton-School/gogo/email` | message construction, MIME rendering, alternatives, attachments, console, dummy, file, memory, and SMTP backends |
| `github.com/Newton-School/gogo/testing` | HTTP clients, settings helpers, database fixtures, fixture assertions, mail assertions, queue assertions, admin assertions, and framework test helpers |
| `github.com/Newton-School/gogo/benchmarks` | benchmark helpers used by the framework |

## Contrib Package Map

| Import path | Feature ownership |
| --- | --- |
| `github.com/Newton-School/gogo/contrib/admindocs` | staff-only admin documentation pages |
| `github.com/Newton-School/gogo/contrib/flatpages` | database-backed flat pages, templates, admin metadata, and site-filtered resolution |
| `github.com/Newton-School/gogo/contrib/flatpages/migrations` | flatpages migration metadata |
| `github.com/Newton-School/gogo/contrib/gis` | GIS fields support, PostGIS functions/lookups, geometry helpers, measurements, layer mapping metadata, and geo sitemap helpers |
| `github.com/Newton-School/gogo/contrib/humanize` | human-readable template filters |
| `github.com/Newton-School/gogo/contrib/postgres` | PostgreSQL search, trigram, indexes, validators, and aggregates |
| `github.com/Newton-School/gogo/contrib/redirects` | database-backed redirect middleware, admin metadata, and safety checks |
| `github.com/Newton-School/gogo/contrib/redirects/migrations` | redirects migration metadata |
| `github.com/Newton-School/gogo/contrib/sitemaps` | sitemap interfaces, sitemap indexes, section rendering, alternates, priorities, and XML output |
| `github.com/Newton-School/gogo/contrib/sites` | site model, current-site lookup, current-site middleware, admin metadata, and site checks |
| `github.com/Newton-School/gogo/contrib/sites/migrations` | sites migration metadata |
| `github.com/Newton-School/gogo/contrib/syndication` | RSS and Atom feed generation |

## CLI Command Surface

Inside a generated project, `go run manage.go <command>` is the explicit
Django-style project entrypoint. The installed `gogo` binary also delegates
project-aware commands to `go run manage.go <command>` when it is run from this
project, so settings, app configs, routes, admin, model metadata, fixtures, and
queue tasks are loaded. Use `gogo startproject` outside an existing project to
create a new project.

| Command | Use for |
| --- | --- |
| `gogo help`, `gogo --help`, `gogo -h` | show commands |
| `gogo version`, `gogo --version`, `gogo -version` | show version metadata |
| `gogo startproject [--force] <name> [path]` | create a project scaffold |
| `gogo <project-aware-command>` | delegate to `go run manage.go <project-aware-command>` when run inside this project |
| `go run manage.go help`, `go run manage.go --help`, `go run manage.go -h` | show project command help |
| `go run manage.go version`, `go run manage.go --version`, `go run manage.go -version` | show framework version metadata |
| `go run manage.go check` | run settings and system checks |
| `go run manage.go check --deploy` | run production readiness checks |
| `go run manage.go runserver` | run the development HTTP server with project HTTP routes, API routes, admin, and development static/media mounts |
| `go run manage.go startapp [--force] <name> apps/<name>` | create and auto-install an app scaffold |
| `go run manage.go makemigrations` | create migration files from generated app metadata |
| `go run manage.go migrate` | apply or roll back migrations |
| `go run manage.go showmigrations` | list migration status |
| `go run manage.go sqlmigrate <app> <migration>` | render migration SQL |
| `go run manage.go squashmigrations <app> <start> <end>` | write a squashed replacement migration with `Replaces` metadata |
| `go run manage.go optimizemigration <app> <migration>` | optimize migration operations or report that no safe optimization is needed |
| `go run manage.go createsuperuser` | create an admin user in `.gogo/auth_users.json` for generated admin login |
| `go run manage.go changepassword` | change a user password in local CLI auth storage |
| `go run manage.go collectstatic` | collect static assets from project and app static roots |
| `go run manage.go shell` | open a project shell |
| `go run manage.go dbshell` | open a database shell |
| `go run manage.go test` | run project tests |
| `go run manage.go worker` | run a queue worker with project task registrations |
| `go run manage.go beat` | run the queue scheduler |
| `go run manage.go inspect` | inspect queue workers and project registered tasks |
| `go run manage.go queues` | inspect queues |
| `go run manage.go dumpdata` | dump fixture data |
| `go run manage.go loaddata` | load fixture data |

Generated project Make commands:

```bash
make test
make check
make run
make tidy
```

## Feature Discovery Workflow For Agents

1. Identify the feature area and route to the matching `.agent/rules/gogo/*`
   rule file.
2. Use the package map above to choose the owning public package.
3. Run `go doc` on the public package or inspect the installed module source
   when exact symbols, options, or examples are needed.
4. Keep app code inside `apps/<app>/` and project wiring inside
   `{{.ProjectName}}/`.
5. Update tests and run focused verification for every behavior change.
6. Do not import internal Gogo packages or copy framework internals into the
   client project.
